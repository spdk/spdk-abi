/* SPDX-License-Identifier: BSD-3-Clause */

#ifndef SPDK_RPC_AUTOGEN_H_
#define SPDK_RPC_AUTOGEN_H_

/*
 * WARNING: This file is auto-generated.
 * Do NOT edit this file manually.
 * Generated by scripts/genrpc.py.
 */

#include "spdk/stdinc.h"

#include "spdk/accel.h"
#include "spdk/accel_module.h"
#include "spdk/bdev.h"
#include "spdk/jsonrpc.h"
#include "spdk/log.h"
#include "spdk/module/accel/dpdk_cryptodev.h"
#include "spdk/module/accel/mlx5.h"
#include "spdk/module/bdev/delay.h"
#include "spdk/module/bdev/error.h"
#include "spdk/module/bdev/nvme.h"
#include "spdk/module/bdev/raid.h"
#include "spdk/nvme_spec.h"
#include "spdk/nvmf.h"
#include "spdk/nvmf_spec.h"

#ifdef __cplusplus
extern "C" {
#endif

/*
 * Forward-declare every generated struct up front.
 *
 * Object and array struct definitions are emitted in schema-iteration
 * order, but a field of type "array" expands to `struct rpc_<class> *`
 * (and an embedded-object field can reference another object's struct).
 * The referenced struct may be defined later in the same header — or,
 * when an object and an array reference each other, there is no
 * ordering that satisfies both. Emitting forward declarations for all
 * `rpc_<object>` and `rpc_<array>` names first makes the header order-
 * independent and lets pointer fields compile regardless of where the
 * full definition lands.
 */
struct rpc_bdev_nvme_multipath_opts;
struct rpc_iscsi_auth_secret;
struct rpc_iscsi_pg_ig_map;
struct rpc_iscsi_portal;
struct rpc_scsi_lun;
struct rpc_nvmf_listen_address;
struct rpc_nvmf_namespace;
struct rpc_nvmf_admin_cmd_passthru;
struct rpc_vhost_blk_controller_params;
struct rpc_vhost_scsi_controller_params;
struct rpc_bdev_histogram_borders;
struct rpc_bdev_iostat_names;
struct rpc_iscsi_initiators;
struct rpc_iscsi_netmasks;
struct rpc_iscsi_pg_ig_maps;
struct rpc_scsi_luns;
struct rpc_iscsi_portals;
struct rpc_iscsi_auth_secrets;
struct rpc_dhchap_digests;
struct rpc_dhchap_dhgroups;
struct rpc_oncs_features;
struct rpc_fuses_features;
struct rpc_raid_base_bdevs;

/* Enum definitions section */


enum rpc_bdev_nvme_cmd_type {
    RPC_BDEV_NVME_CMD_TYPE_ADMIN = 1,
    RPC_BDEV_NVME_CMD_TYPE_IO = 2,
};

enum rpc_bdev_nvme_data_direction {
    RPC_BDEV_NVME_DATA_DIRECTION_H2C = SPDK_NVME_DATA_HOST_TO_CONTROLLER,
    RPC_BDEV_NVME_DATA_DIRECTION_C2H = SPDK_NVME_DATA_CONTROLLER_TO_HOST,
};

enum rpc_bdev_nvme_multipath_policy {
    RPC_BDEV_NVME_MULTIPATH_POLICY_ACTIVE_PASSIVE = SPDK_BDEV_NVME_MULTIPATH_POLICY_ACTIVE_PASSIVE,
    RPC_BDEV_NVME_MULTIPATH_POLICY_ACTIVE_ACTIVE = SPDK_BDEV_NVME_MULTIPATH_POLICY_ACTIVE_ACTIVE,
};

enum rpc_bdev_nvme_multipath_selector {
    RPC_BDEV_NVME_MULTIPATH_SELECTOR_ROUND_ROBIN = SPDK_BDEV_NVME_MULTIPATH_SELECTOR_ROUND_ROBIN,
    RPC_BDEV_NVME_MULTIPATH_SELECTOR_QUEUE_DEPTH = SPDK_BDEV_NVME_MULTIPATH_SELECTOR_QUEUE_DEPTH,
};

enum rpc_bdev_nvme_multipath_mode {
    RPC_BDEV_NVME_MULTIPATH_MODE_FAILOVER = 0,
    RPC_BDEV_NVME_MULTIPATH_MODE_MULTIPATH = 1,
    RPC_BDEV_NVME_MULTIPATH_MODE_DISABLE = 2,
};

enum rpc_bdev_nvme_timeout_action {
    RPC_BDEV_NVME_TIMEOUT_ACTION_NONE = SPDK_BDEV_NVME_TIMEOUT_ACTION_NONE,
    RPC_BDEV_NVME_TIMEOUT_ACTION_RESET = SPDK_BDEV_NVME_TIMEOUT_ACTION_RESET,
    RPC_BDEV_NVME_TIMEOUT_ACTION_ABORT = SPDK_BDEV_NVME_TIMEOUT_ACTION_ABORT,
};

enum rpc_bdev_error_inject_io_type {
    RPC_BDEV_ERROR_INJECT_IO_TYPE_CLEAR = 0,
    RPC_BDEV_ERROR_INJECT_IO_TYPE_READ = SPDK_BDEV_IO_TYPE_READ,
    RPC_BDEV_ERROR_INJECT_IO_TYPE_WRITE = SPDK_BDEV_IO_TYPE_WRITE,
    RPC_BDEV_ERROR_INJECT_IO_TYPE_UNMAP = SPDK_BDEV_IO_TYPE_UNMAP,
    RPC_BDEV_ERROR_INJECT_IO_TYPE_FLUSH = SPDK_BDEV_IO_TYPE_FLUSH,
    RPC_BDEV_ERROR_INJECT_IO_TYPE_ALL = UINT32_MAX,
};

enum rpc_bdev_error_inject_error_type {
    RPC_BDEV_ERROR_INJECT_ERROR_TYPE_FAILURE = SPDK_BDEV_ERROR_TYPE_FAILURE,
    RPC_BDEV_ERROR_INJECT_ERROR_TYPE_PENDING = SPDK_BDEV_ERROR_TYPE_PENDING,
    RPC_BDEV_ERROR_INJECT_ERROR_TYPE_CORRUPT_DATA = SPDK_BDEV_ERROR_TYPE_CORRUPT_DATA,
    RPC_BDEV_ERROR_INJECT_ERROR_TYPE_NOMEM = SPDK_BDEV_ERROR_TYPE_NOMEM,
    RPC_BDEV_ERROR_INJECT_ERROR_TYPE_NVME_FAILURE = SPDK_BDEV_ERROR_TYPE_NVME_FAILURE,
};

enum rpc_bdev_reset_stat_mode {
    RPC_BDEV_RESET_STAT_MODE_ALL = SPDK_BDEV_RESET_STAT_ALL,
    RPC_BDEV_RESET_STAT_MODE_MAXMIN = SPDK_BDEV_RESET_STAT_MAXMIN,
    RPC_BDEV_RESET_STAT_MODE_ERROR = SPDK_BDEV_RESET_STAT_ERROR,
    RPC_BDEV_RESET_STAT_MODE_NONE = SPDK_BDEV_RESET_STAT_NONE,
};

enum rpc_nvme_ana_state {
    RPC_NVME_ANA_STATE_OPTIMIZED = SPDK_NVME_ANA_OPTIMIZED_STATE,
    RPC_NVME_ANA_STATE_NON_OPTIMIZED = SPDK_NVME_ANA_NON_OPTIMIZED_STATE,
    RPC_NVME_ANA_STATE_INACCESSIBLE = SPDK_NVME_ANA_INACCESSIBLE_STATE,
};

enum rpc_accel_error_opcode {
    RPC_ACCEL_ERROR_OPCODE_COPY = SPDK_ACCEL_OPC_COPY,
    RPC_ACCEL_ERROR_OPCODE_FILL = SPDK_ACCEL_OPC_FILL,
    RPC_ACCEL_ERROR_OPCODE_DUALCAST = SPDK_ACCEL_OPC_DUALCAST,
    RPC_ACCEL_ERROR_OPCODE_COMPARE = SPDK_ACCEL_OPC_COMPARE,
    RPC_ACCEL_ERROR_OPCODE_CRC32C = SPDK_ACCEL_OPC_CRC32C,
    RPC_ACCEL_ERROR_OPCODE_COPY_CRC32C = SPDK_ACCEL_OPC_COPY_CRC32C,
    RPC_ACCEL_ERROR_OPCODE_COMPRESS = SPDK_ACCEL_OPC_COMPRESS,
    RPC_ACCEL_ERROR_OPCODE_DECOMPRESS = SPDK_ACCEL_OPC_DECOMPRESS,
    RPC_ACCEL_ERROR_OPCODE_ENCRYPT = SPDK_ACCEL_OPC_ENCRYPT,
    RPC_ACCEL_ERROR_OPCODE_DECRYPT = SPDK_ACCEL_OPC_DECRYPT,
    RPC_ACCEL_ERROR_OPCODE_XOR = SPDK_ACCEL_OPC_XOR,
    RPC_ACCEL_ERROR_OPCODE_DIF_VERIFY = SPDK_ACCEL_OPC_DIF_VERIFY,
    RPC_ACCEL_ERROR_OPCODE_DIF_VERIFY_COPY = SPDK_ACCEL_OPC_DIF_VERIFY_COPY,
    RPC_ACCEL_ERROR_OPCODE_DIF_GENERATE = SPDK_ACCEL_OPC_DIF_GENERATE,
    RPC_ACCEL_ERROR_OPCODE_DIF_GENERATE_COPY = SPDK_ACCEL_OPC_DIF_GENERATE_COPY,
    RPC_ACCEL_ERROR_OPCODE_DIX_GENERATE = SPDK_ACCEL_OPC_DIX_GENERATE,
    RPC_ACCEL_ERROR_OPCODE_DIX_VERIFY = SPDK_ACCEL_OPC_DIX_VERIFY,
};

enum rpc_accel_error_inject_type {
    RPC_ACCEL_ERROR_INJECT_TYPE_DISABLE = 0,
    RPC_ACCEL_ERROR_INJECT_TYPE_CORRUPT = 1,
    RPC_ACCEL_ERROR_INJECT_TYPE_FAILURE = 2,
};

enum rpc_bdev_raid_level {
    RPC_BDEV_RAID_LEVEL_RAID0 = SPDK_BDEV_RAID_LEVEL_RAID0,
    RPC_BDEV_RAID_LEVEL_0 = SPDK_BDEV_RAID_LEVEL_RAID0,
    RPC_BDEV_RAID_LEVEL_RAID1 = SPDK_BDEV_RAID_LEVEL_RAID1,
    RPC_BDEV_RAID_LEVEL_1 = SPDK_BDEV_RAID_LEVEL_RAID1,
    RPC_BDEV_RAID_LEVEL_RAID5F = SPDK_BDEV_RAID_LEVEL_RAID5F,
    RPC_BDEV_RAID_LEVEL_5F = SPDK_BDEV_RAID_LEVEL_RAID5F,
    RPC_BDEV_RAID_LEVEL_CONCAT = SPDK_BDEV_RAID_LEVEL_CONCAT,
};

enum rpc_bdev_raid_state {
    RPC_BDEV_RAID_STATE_ONLINE = SPDK_BDEV_RAID_STATE_ONLINE,
    RPC_BDEV_RAID_STATE_CONFIGURING = SPDK_BDEV_RAID_STATE_CONFIGURING,
    RPC_BDEV_RAID_STATE_OFFLINE = SPDK_BDEV_RAID_STATE_OFFLINE,
    RPC_BDEV_RAID_STATE_ALL = SPDK_BDEV_RAID_STATE_MAX,
};

enum rpc_dhchap_digest {
    RPC_DHCHAP_DIGEST_SHA256 = SPDK_NVMF_DHCHAP_HASH_SHA256,
    RPC_DHCHAP_DIGEST_SHA384 = SPDK_NVMF_DHCHAP_HASH_SHA384,
    RPC_DHCHAP_DIGEST_SHA512 = SPDK_NVMF_DHCHAP_HASH_SHA512,
};

enum rpc_dhchap_dhgroup {
    RPC_DHCHAP_DHGROUP_NULL = SPDK_NVMF_DHCHAP_DHGROUP_NULL,
    RPC_DHCHAP_DHGROUP_FFDHE2048 = SPDK_NVMF_DHCHAP_DHGROUP_2048,
    RPC_DHCHAP_DHGROUP_FFDHE3072 = SPDK_NVMF_DHCHAP_DHGROUP_3072,
    RPC_DHCHAP_DHGROUP_FFDHE4096 = SPDK_NVMF_DHCHAP_DHGROUP_4096,
    RPC_DHCHAP_DHGROUP_FFDHE6144 = SPDK_NVMF_DHCHAP_DHGROUP_6144,
    RPC_DHCHAP_DHGROUP_FFDHE8192 = SPDK_NVMF_DHCHAP_DHGROUP_8192,
};

enum rpc_nvmf_dup_host_policy {
    RPC_NVMF_DUP_HOST_POLICY_ALLOW = SPDK_NVMF_SUBSYSTEM_DUP_HOST_POLICY_ALLOW,
    RPC_NVMF_DUP_HOST_POLICY_RESTRICT_PER_LISTENER = SPDK_NVMF_SUBSYSTEM_DUP_HOST_POLICY_RESTRICT_PER_LISTENER,
};

enum rpc_oncs_feature {
    RPC_ONCS_FEATURE_NVMCMPS = 0,
    RPC_ONCS_FEATURE_NVMDSMSV = 2,
    RPC_ONCS_FEATURE_NVMWZSV = 3,
    RPC_ONCS_FEATURE_RESERVS = 5,
    RPC_ONCS_FEATURE_NVMCPYS = 8,
};

enum rpc_fuses_feature {
    RPC_FUSES_FEATURE_FCWS = 0,
};

enum rpc_bdev_delay_latency_type {
    RPC_BDEV_DELAY_LATENCY_TYPE_AVG_READ = SPDK_BDEV_DELAY_IO_TYPE_AVG_READ,
    RPC_BDEV_DELAY_LATENCY_TYPE_P99_READ = SPDK_BDEV_DELAY_IO_TYPE_P99_READ,
    RPC_BDEV_DELAY_LATENCY_TYPE_AVG_WRITE = SPDK_BDEV_DELAY_IO_TYPE_AVG_WRITE,
    RPC_BDEV_DELAY_LATENCY_TYPE_P99_WRITE = SPDK_BDEV_DELAY_IO_TYPE_P99_WRITE,
};

enum rpc_accel_crypto_tweak_mode {
    RPC_ACCEL_CRYPTO_TWEAK_MODE_SIMPLE_LBA = SPDK_ACCEL_CRYPTO_TWEAK_MODE_SIMPLE_LBA,
    RPC_ACCEL_CRYPTO_TWEAK_MODE_JOIN_NEG_LBA_WITH_LBA = SPDK_ACCEL_CRYPTO_TWEAK_MODE_JOIN_NEG_LBA_WITH_LBA,
    RPC_ACCEL_CRYPTO_TWEAK_MODE_INCR_512_FULL_LBA = SPDK_ACCEL_CRYPTO_TWEAK_MODE_INCR_512_FULL_LBA,
    RPC_ACCEL_CRYPTO_TWEAK_MODE_INCR_512_UPPER_LBA = SPDK_ACCEL_CRYPTO_TWEAK_MODE_INCR_512_UPPER_LBA,
};

enum rpc_accel_crypto_cipher {
    RPC_ACCEL_CRYPTO_CIPHER_AES_CBC = SPDK_ACCEL_CIPHER_AES_CBC,
    RPC_ACCEL_CRYPTO_CIPHER_AES_XTS = SPDK_ACCEL_CIPHER_AES_XTS,
};

enum rpc_bdev_virtio_trtype {
    RPC_BDEV_VIRTIO_TRTYPE_PCI = 0,
    RPC_BDEV_VIRTIO_TRTYPE_USER = 1,
    RPC_BDEV_VIRTIO_TRTYPE_VFIO_USER = 2,
};

enum rpc_bdev_virtio_dev_type {
    RPC_BDEV_VIRTIO_DEV_TYPE_BLK = 0,
    RPC_BDEV_VIRTIO_DEV_TYPE_SCSI = 1,
};

enum rpc_accel_mlx5_dump_state_level {
    RPC_ACCEL_MLX5_DUMP_STATE_LEVEL_TOTAL = SPDK_ACCEL_MLX5_DUMP_STAT_LEVEL_TOTAL,
    RPC_ACCEL_MLX5_DUMP_STATE_LEVEL_CHANNEL = SPDK_ACCEL_MLX5_DUMP_STAT_LEVEL_CHANNEL,
    RPC_ACCEL_MLX5_DUMP_STATE_LEVEL_DEVICE = SPDK_ACCEL_MLX5_DUMP_STAT_LEVEL_DEVICE,
};

enum rpc_log_level {
    RPC_LOG_LEVEL_ERROR = SPDK_LOG_ERROR,
    RPC_LOG_LEVEL_WARNING = SPDK_LOG_WARN,
    RPC_LOG_LEVEL_NOTICE = SPDK_LOG_NOTICE,
    RPC_LOG_LEVEL_INFO = SPDK_LOG_INFO,
    RPC_LOG_LEVEL_DEBUG = SPDK_LOG_DEBUG,
};

enum rpc_kill_signal {
    RPC_KILL_SIGNAL_SIGINT = SIGINT,
    RPC_KILL_SIGNAL_SIGTERM = SIGTERM,
    RPC_KILL_SIGNAL_SIGQUIT = SIGQUIT,
    RPC_KILL_SIGNAL_SIGHUP = SIGHUP,
    RPC_KILL_SIGNAL_SIGKILL = SIGKILL,
    RPC_KILL_SIGNAL_SIGUSR1 = SIGUSR1,
};

enum rpc_dpdk_cryptodev_driver {
    RPC_DPDK_CRYPTODEV_DRIVER_CRYPTO_AESNI_MB = SPDK_ACCEL_DPDK_CRYPTODEV_DRIVER_AESNI_MB,
    RPC_DPDK_CRYPTODEV_DRIVER_CRYPTO_QAT = SPDK_ACCEL_DPDK_CRYPTODEV_DRIVER_QAT,
    RPC_DPDK_CRYPTODEV_DRIVER_MLX5_PCI = SPDK_ACCEL_DPDK_CRYPTODEV_DRIVER_MLX5_PCI,
    RPC_DPDK_CRYPTODEV_DRIVER_CRYPTO_UADK = SPDK_ACCEL_DPDK_CRYPTODEV_DRIVER_UADK,
};


/* Enum decoder functions section */


static inline int
rpc_decode_bdev_nvme_cmd_type(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_nvme_cmd_type *p = out;

	if (spdk_json_strequal(val, "admin")) {
		*p = RPC_BDEV_NVME_CMD_TYPE_ADMIN;
	} else if (spdk_json_strequal(val, "io")) {
		*p = RPC_BDEV_NVME_CMD_TYPE_IO;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_nvme_cmd_type value: '%s'. Supported values: admin, io\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_nvme_data_direction(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_nvme_data_direction *p = out;

	if (spdk_json_strequal(val, "h2c")) {
		*p = RPC_BDEV_NVME_DATA_DIRECTION_H2C;
	} else if (spdk_json_strequal(val, "c2h")) {
		*p = RPC_BDEV_NVME_DATA_DIRECTION_C2H;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_nvme_data_direction value: '%s'. Supported values: h2c, c2h\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_nvme_multipath_policy(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_nvme_multipath_policy *p = out;

	if (spdk_json_strequal(val, "active_passive")) {
		*p = RPC_BDEV_NVME_MULTIPATH_POLICY_ACTIVE_PASSIVE;
	} else if (spdk_json_strequal(val, "active_active")) {
		*p = RPC_BDEV_NVME_MULTIPATH_POLICY_ACTIVE_ACTIVE;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_nvme_multipath_policy value: '%s'. Supported values: active_passive, active_active\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_nvme_multipath_selector(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_nvme_multipath_selector *p = out;

	if (spdk_json_strequal(val, "round_robin")) {
		*p = RPC_BDEV_NVME_MULTIPATH_SELECTOR_ROUND_ROBIN;
	} else if (spdk_json_strequal(val, "queue_depth")) {
		*p = RPC_BDEV_NVME_MULTIPATH_SELECTOR_QUEUE_DEPTH;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_nvme_multipath_selector value: '%s'. Supported values: round_robin, queue_depth\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_nvme_multipath_mode(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_nvme_multipath_mode *p = out;

	if (spdk_json_strequal(val, "failover")) {
		*p = RPC_BDEV_NVME_MULTIPATH_MODE_FAILOVER;
	} else if (spdk_json_strequal(val, "multipath")) {
		*p = RPC_BDEV_NVME_MULTIPATH_MODE_MULTIPATH;
	} else if (spdk_json_strequal(val, "disable")) {
		*p = RPC_BDEV_NVME_MULTIPATH_MODE_DISABLE;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_nvme_multipath_mode value: '%s'. Supported values: failover, multipath, disable\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_nvme_timeout_action(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_nvme_timeout_action *p = out;

	if (spdk_json_strequal(val, "none")) {
		*p = RPC_BDEV_NVME_TIMEOUT_ACTION_NONE;
	} else if (spdk_json_strequal(val, "reset")) {
		*p = RPC_BDEV_NVME_TIMEOUT_ACTION_RESET;
	} else if (spdk_json_strequal(val, "abort")) {
		*p = RPC_BDEV_NVME_TIMEOUT_ACTION_ABORT;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_nvme_timeout_action value: '%s'. Supported values: none, reset, abort\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_error_inject_io_type(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_error_inject_io_type *p = out;

	if (spdk_json_strequal(val, "clear")) {
		*p = RPC_BDEV_ERROR_INJECT_IO_TYPE_CLEAR;
	} else if (spdk_json_strequal(val, "read")) {
		*p = RPC_BDEV_ERROR_INJECT_IO_TYPE_READ;
	} else if (spdk_json_strequal(val, "write")) {
		*p = RPC_BDEV_ERROR_INJECT_IO_TYPE_WRITE;
	} else if (spdk_json_strequal(val, "unmap")) {
		*p = RPC_BDEV_ERROR_INJECT_IO_TYPE_UNMAP;
	} else if (spdk_json_strequal(val, "flush")) {
		*p = RPC_BDEV_ERROR_INJECT_IO_TYPE_FLUSH;
	} else if (spdk_json_strequal(val, "all")) {
		*p = RPC_BDEV_ERROR_INJECT_IO_TYPE_ALL;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_error_inject_io_type value: '%s'. Supported values: clear, read, write, unmap, flush, all\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_error_inject_error_type(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_error_inject_error_type *p = out;

	if (spdk_json_strequal(val, "failure")) {
		*p = RPC_BDEV_ERROR_INJECT_ERROR_TYPE_FAILURE;
	} else if (spdk_json_strequal(val, "pending")) {
		*p = RPC_BDEV_ERROR_INJECT_ERROR_TYPE_PENDING;
	} else if (spdk_json_strequal(val, "corrupt_data")) {
		*p = RPC_BDEV_ERROR_INJECT_ERROR_TYPE_CORRUPT_DATA;
	} else if (spdk_json_strequal(val, "nomem")) {
		*p = RPC_BDEV_ERROR_INJECT_ERROR_TYPE_NOMEM;
	} else if (spdk_json_strequal(val, "nvme_failure")) {
		*p = RPC_BDEV_ERROR_INJECT_ERROR_TYPE_NVME_FAILURE;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_error_inject_error_type value: '%s'. Supported values: failure, pending, corrupt_data, nomem, nvme_failure\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_reset_stat_mode(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_reset_stat_mode *p = out;

	if (spdk_json_strequal(val, "all")) {
		*p = RPC_BDEV_RESET_STAT_MODE_ALL;
	} else if (spdk_json_strequal(val, "maxmin")) {
		*p = RPC_BDEV_RESET_STAT_MODE_MAXMIN;
	} else if (spdk_json_strequal(val, "error")) {
		*p = RPC_BDEV_RESET_STAT_MODE_ERROR;
	} else if (spdk_json_strequal(val, "none")) {
		*p = RPC_BDEV_RESET_STAT_MODE_NONE;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_reset_stat_mode value: '%s'. Supported values: all, maxmin, error, none\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_nvme_ana_state(const struct spdk_json_val *val, void *out)
{
	enum rpc_nvme_ana_state *p = out;

	if (spdk_json_strequal(val, "optimized")) {
		*p = RPC_NVME_ANA_STATE_OPTIMIZED;
	} else if (spdk_json_strequal(val, "non_optimized")) {
		*p = RPC_NVME_ANA_STATE_NON_OPTIMIZED;
	} else if (spdk_json_strequal(val, "inaccessible")) {
		*p = RPC_NVME_ANA_STATE_INACCESSIBLE;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid nvme_ana_state value: '%s'. Supported values: optimized, non_optimized, inaccessible\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_accel_error_opcode(const struct spdk_json_val *val, void *out)
{
	enum rpc_accel_error_opcode *p = out;

	if (spdk_json_strequal(val, "copy")) {
		*p = RPC_ACCEL_ERROR_OPCODE_COPY;
	} else if (spdk_json_strequal(val, "fill")) {
		*p = RPC_ACCEL_ERROR_OPCODE_FILL;
	} else if (spdk_json_strequal(val, "dualcast")) {
		*p = RPC_ACCEL_ERROR_OPCODE_DUALCAST;
	} else if (spdk_json_strequal(val, "compare")) {
		*p = RPC_ACCEL_ERROR_OPCODE_COMPARE;
	} else if (spdk_json_strequal(val, "crc32c")) {
		*p = RPC_ACCEL_ERROR_OPCODE_CRC32C;
	} else if (spdk_json_strequal(val, "copy_crc32c")) {
		*p = RPC_ACCEL_ERROR_OPCODE_COPY_CRC32C;
	} else if (spdk_json_strequal(val, "compress")) {
		*p = RPC_ACCEL_ERROR_OPCODE_COMPRESS;
	} else if (spdk_json_strequal(val, "decompress")) {
		*p = RPC_ACCEL_ERROR_OPCODE_DECOMPRESS;
	} else if (spdk_json_strequal(val, "encrypt")) {
		*p = RPC_ACCEL_ERROR_OPCODE_ENCRYPT;
	} else if (spdk_json_strequal(val, "decrypt")) {
		*p = RPC_ACCEL_ERROR_OPCODE_DECRYPT;
	} else if (spdk_json_strequal(val, "xor")) {
		*p = RPC_ACCEL_ERROR_OPCODE_XOR;
	} else if (spdk_json_strequal(val, "dif_verify")) {
		*p = RPC_ACCEL_ERROR_OPCODE_DIF_VERIFY;
	} else if (spdk_json_strequal(val, "dif_verify_copy")) {
		*p = RPC_ACCEL_ERROR_OPCODE_DIF_VERIFY_COPY;
	} else if (spdk_json_strequal(val, "dif_generate")) {
		*p = RPC_ACCEL_ERROR_OPCODE_DIF_GENERATE;
	} else if (spdk_json_strequal(val, "dif_generate_copy")) {
		*p = RPC_ACCEL_ERROR_OPCODE_DIF_GENERATE_COPY;
	} else if (spdk_json_strequal(val, "dix_generate")) {
		*p = RPC_ACCEL_ERROR_OPCODE_DIX_GENERATE;
	} else if (spdk_json_strequal(val, "dix_verify")) {
		*p = RPC_ACCEL_ERROR_OPCODE_DIX_VERIFY;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid accel_error_opcode value: '%s'. Supported values: copy, fill, dualcast, compare, crc32c, copy_crc32c, compress, decompress, encrypt, decrypt, xor, dif_verify, dif_verify_copy, dif_generate, dif_generate_copy, dix_generate, dix_verify\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_accel_error_inject_type(const struct spdk_json_val *val, void *out)
{
	enum rpc_accel_error_inject_type *p = out;

	if (spdk_json_strequal(val, "disable")) {
		*p = RPC_ACCEL_ERROR_INJECT_TYPE_DISABLE;
	} else if (spdk_json_strequal(val, "corrupt")) {
		*p = RPC_ACCEL_ERROR_INJECT_TYPE_CORRUPT;
	} else if (spdk_json_strequal(val, "failure")) {
		*p = RPC_ACCEL_ERROR_INJECT_TYPE_FAILURE;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid accel_error_inject_type value: '%s'. Supported values: disable, corrupt, failure\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_raid_level(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_raid_level *p = out;

	if (spdk_json_strequal(val, "raid0")) {
		*p = RPC_BDEV_RAID_LEVEL_RAID0;
	} else if (spdk_json_strequal(val, "0")) {
		*p = RPC_BDEV_RAID_LEVEL_0;
	} else if (spdk_json_strequal(val, "raid1")) {
		*p = RPC_BDEV_RAID_LEVEL_RAID1;
	} else if (spdk_json_strequal(val, "1")) {
		*p = RPC_BDEV_RAID_LEVEL_1;
	} else if (spdk_json_strequal(val, "raid5f")) {
		*p = RPC_BDEV_RAID_LEVEL_RAID5F;
	} else if (spdk_json_strequal(val, "5f")) {
		*p = RPC_BDEV_RAID_LEVEL_5F;
	} else if (spdk_json_strequal(val, "concat")) {
		*p = RPC_BDEV_RAID_LEVEL_CONCAT;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_raid_level value: '%s'. Supported values: raid0, 0, raid1, 1, raid5f, 5f, concat\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_raid_state(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_raid_state *p = out;

	if (spdk_json_strequal(val, "online")) {
		*p = RPC_BDEV_RAID_STATE_ONLINE;
	} else if (spdk_json_strequal(val, "configuring")) {
		*p = RPC_BDEV_RAID_STATE_CONFIGURING;
	} else if (spdk_json_strequal(val, "offline")) {
		*p = RPC_BDEV_RAID_STATE_OFFLINE;
	} else if (spdk_json_strequal(val, "all")) {
		*p = RPC_BDEV_RAID_STATE_ALL;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_raid_state value: '%s'. Supported values: online, configuring, offline, all\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_dhchap_digest(const struct spdk_json_val *val, void *out)
{
	enum rpc_dhchap_digest *p = out;

	if (spdk_json_strequal(val, "sha256")) {
		*p = RPC_DHCHAP_DIGEST_SHA256;
	} else if (spdk_json_strequal(val, "sha384")) {
		*p = RPC_DHCHAP_DIGEST_SHA384;
	} else if (spdk_json_strequal(val, "sha512")) {
		*p = RPC_DHCHAP_DIGEST_SHA512;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid dhchap_digest value: '%s'. Supported values: sha256, sha384, sha512\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_dhchap_dhgroup(const struct spdk_json_val *val, void *out)
{
	enum rpc_dhchap_dhgroup *p = out;

	if (spdk_json_strequal(val, "null")) {
		*p = RPC_DHCHAP_DHGROUP_NULL;
	} else if (spdk_json_strequal(val, "ffdhe2048")) {
		*p = RPC_DHCHAP_DHGROUP_FFDHE2048;
	} else if (spdk_json_strequal(val, "ffdhe3072")) {
		*p = RPC_DHCHAP_DHGROUP_FFDHE3072;
	} else if (spdk_json_strequal(val, "ffdhe4096")) {
		*p = RPC_DHCHAP_DHGROUP_FFDHE4096;
	} else if (spdk_json_strequal(val, "ffdhe6144")) {
		*p = RPC_DHCHAP_DHGROUP_FFDHE6144;
	} else if (spdk_json_strequal(val, "ffdhe8192")) {
		*p = RPC_DHCHAP_DHGROUP_FFDHE8192;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid dhchap_dhgroup value: '%s'. Supported values: null, ffdhe2048, ffdhe3072, ffdhe4096, ffdhe6144, ffdhe8192\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_nvmf_dup_host_policy(const struct spdk_json_val *val, void *out)
{
	enum rpc_nvmf_dup_host_policy *p = out;

	if (spdk_json_strequal(val, "allow")) {
		*p = RPC_NVMF_DUP_HOST_POLICY_ALLOW;
	} else if (spdk_json_strequal(val, "restrict_per_listener")) {
		*p = RPC_NVMF_DUP_HOST_POLICY_RESTRICT_PER_LISTENER;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid nvmf_dup_host_policy value: '%s'. Supported values: allow, restrict_per_listener\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_oncs_feature(const struct spdk_json_val *val, void *out)
{
	enum rpc_oncs_feature *p = out;

	if (spdk_json_strequal(val, "nvmcmps")) {
		*p = RPC_ONCS_FEATURE_NVMCMPS;
	} else if (spdk_json_strequal(val, "nvmdsmsv")) {
		*p = RPC_ONCS_FEATURE_NVMDSMSV;
	} else if (spdk_json_strequal(val, "nvmwzsv")) {
		*p = RPC_ONCS_FEATURE_NVMWZSV;
	} else if (spdk_json_strequal(val, "reservs")) {
		*p = RPC_ONCS_FEATURE_RESERVS;
	} else if (spdk_json_strequal(val, "nvmcpys")) {
		*p = RPC_ONCS_FEATURE_NVMCPYS;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid oncs_feature value: '%s'. Supported values: nvmcmps, nvmdsmsv, nvmwzsv, reservs, nvmcpys\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_fuses_feature(const struct spdk_json_val *val, void *out)
{
	enum rpc_fuses_feature *p = out;

	if (spdk_json_strequal(val, "fcws")) {
		*p = RPC_FUSES_FEATURE_FCWS;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid fuses_feature value: '%s'. Supported values: fcws\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_delay_latency_type(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_delay_latency_type *p = out;

	if (spdk_json_strequal(val, "avg_read")) {
		*p = RPC_BDEV_DELAY_LATENCY_TYPE_AVG_READ;
	} else if (spdk_json_strequal(val, "p99_read")) {
		*p = RPC_BDEV_DELAY_LATENCY_TYPE_P99_READ;
	} else if (spdk_json_strequal(val, "avg_write")) {
		*p = RPC_BDEV_DELAY_LATENCY_TYPE_AVG_WRITE;
	} else if (spdk_json_strequal(val, "p99_write")) {
		*p = RPC_BDEV_DELAY_LATENCY_TYPE_P99_WRITE;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_delay_latency_type value: '%s'. Supported values: avg_read, p99_read, avg_write, p99_write\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_accel_crypto_tweak_mode(const struct spdk_json_val *val, void *out)
{
	enum rpc_accel_crypto_tweak_mode *p = out;

	if (spdk_json_strequal(val, "SIMPLE_LBA")) {
		*p = RPC_ACCEL_CRYPTO_TWEAK_MODE_SIMPLE_LBA;
	} else if (spdk_json_strequal(val, "JOIN_NEG_LBA_WITH_LBA")) {
		*p = RPC_ACCEL_CRYPTO_TWEAK_MODE_JOIN_NEG_LBA_WITH_LBA;
	} else if (spdk_json_strequal(val, "INCR_512_FULL_LBA")) {
		*p = RPC_ACCEL_CRYPTO_TWEAK_MODE_INCR_512_FULL_LBA;
	} else if (spdk_json_strequal(val, "INCR_512_UPPER_LBA")) {
		*p = RPC_ACCEL_CRYPTO_TWEAK_MODE_INCR_512_UPPER_LBA;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid accel_crypto_tweak_mode value: '%s'. Supported values: SIMPLE_LBA, JOIN_NEG_LBA_WITH_LBA, INCR_512_FULL_LBA, INCR_512_UPPER_LBA\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_accel_crypto_cipher(const struct spdk_json_val *val, void *out)
{
	enum rpc_accel_crypto_cipher *p = out;

	if (spdk_json_strequal(val, "AES_CBC")) {
		*p = RPC_ACCEL_CRYPTO_CIPHER_AES_CBC;
	} else if (spdk_json_strequal(val, "AES_XTS")) {
		*p = RPC_ACCEL_CRYPTO_CIPHER_AES_XTS;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid accel_crypto_cipher value: '%s'. Supported values: AES_CBC, AES_XTS\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_virtio_trtype(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_virtio_trtype *p = out;

	if (spdk_json_strequal(val, "pci")) {
		*p = RPC_BDEV_VIRTIO_TRTYPE_PCI;
	} else if (spdk_json_strequal(val, "user")) {
		*p = RPC_BDEV_VIRTIO_TRTYPE_USER;
	} else if (spdk_json_strequal(val, "vfio-user")) {
		*p = RPC_BDEV_VIRTIO_TRTYPE_VFIO_USER;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_virtio_trtype value: '%s'. Supported values: pci, user, vfio-user\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_bdev_virtio_dev_type(const struct spdk_json_val *val, void *out)
{
	enum rpc_bdev_virtio_dev_type *p = out;

	if (spdk_json_strequal(val, "blk")) {
		*p = RPC_BDEV_VIRTIO_DEV_TYPE_BLK;
	} else if (spdk_json_strequal(val, "scsi")) {
		*p = RPC_BDEV_VIRTIO_DEV_TYPE_SCSI;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid bdev_virtio_dev_type value: '%s'. Supported values: blk, scsi\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_accel_mlx5_dump_state_level(const struct spdk_json_val *val, void *out)
{
	enum rpc_accel_mlx5_dump_state_level *p = out;

	if (spdk_json_strequal(val, "total")) {
		*p = RPC_ACCEL_MLX5_DUMP_STATE_LEVEL_TOTAL;
	} else if (spdk_json_strequal(val, "channel")) {
		*p = RPC_ACCEL_MLX5_DUMP_STATE_LEVEL_CHANNEL;
	} else if (spdk_json_strequal(val, "device")) {
		*p = RPC_ACCEL_MLX5_DUMP_STATE_LEVEL_DEVICE;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid accel_mlx5_dump_state_level value: '%s'. Supported values: total, channel, device\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_log_level(const struct spdk_json_val *val, void *out)
{
	enum rpc_log_level *p = out;

	if (spdk_json_strequal(val, "ERROR")) {
		*p = RPC_LOG_LEVEL_ERROR;
	} else if (spdk_json_strequal(val, "WARNING")) {
		*p = RPC_LOG_LEVEL_WARNING;
	} else if (spdk_json_strequal(val, "NOTICE")) {
		*p = RPC_LOG_LEVEL_NOTICE;
	} else if (spdk_json_strequal(val, "INFO")) {
		*p = RPC_LOG_LEVEL_INFO;
	} else if (spdk_json_strequal(val, "DEBUG")) {
		*p = RPC_LOG_LEVEL_DEBUG;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid log_level value: '%s'. Supported values: ERROR, WARNING, NOTICE, INFO, DEBUG\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_kill_signal(const struct spdk_json_val *val, void *out)
{
	enum rpc_kill_signal *p = out;

	if (spdk_json_strequal(val, "SIGINT")) {
		*p = RPC_KILL_SIGNAL_SIGINT;
	} else if (spdk_json_strequal(val, "SIGTERM")) {
		*p = RPC_KILL_SIGNAL_SIGTERM;
	} else if (spdk_json_strequal(val, "SIGQUIT")) {
		*p = RPC_KILL_SIGNAL_SIGQUIT;
	} else if (spdk_json_strequal(val, "SIGHUP")) {
		*p = RPC_KILL_SIGNAL_SIGHUP;
	} else if (spdk_json_strequal(val, "SIGKILL")) {
		*p = RPC_KILL_SIGNAL_SIGKILL;
	} else if (spdk_json_strequal(val, "SIGUSR1")) {
		*p = RPC_KILL_SIGNAL_SIGUSR1;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid kill_signal value: '%s'. Supported values: SIGINT, SIGTERM, SIGQUIT, SIGHUP, SIGKILL, SIGUSR1\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}

static inline int
rpc_decode_dpdk_cryptodev_driver(const struct spdk_json_val *val, void *out)
{
	enum rpc_dpdk_cryptodev_driver *p = out;

	if (spdk_json_strequal(val, "crypto_aesni_mb")) {
		*p = RPC_DPDK_CRYPTODEV_DRIVER_CRYPTO_AESNI_MB;
	} else if (spdk_json_strequal(val, "crypto_qat")) {
		*p = RPC_DPDK_CRYPTODEV_DRIVER_CRYPTO_QAT;
	} else if (spdk_json_strequal(val, "mlx5_pci")) {
		*p = RPC_DPDK_CRYPTODEV_DRIVER_MLX5_PCI;
	} else if (spdk_json_strequal(val, "crypto_uadk")) {
		*p = RPC_DPDK_CRYPTODEV_DRIVER_CRYPTO_UADK;
	} else {
		char *s = spdk_json_strdup(val);
		SPDK_ERRLOG("Invalid dpdk_cryptodev_driver value: '%s'. Supported values: crypto_aesni_mb, crypto_qat, mlx5_pci, crypto_uadk\n", s ? s : "null");
		free(s);
		return -EINVAL;
	}

	return 0;
}


/* Embedded objects section */


extern struct spdk_deprecation *_deprecated_nvmf_namespace_hide_metadata;


static inline int
rpc_decode_hide_metadata(const struct spdk_json_val *val, void *out)
{
	SPDK_LOG_DEPRECATED(nvmf_namespace_hide_metadata);
	return spdk_json_decode_bool(val, out);
}



struct rpc_bdev_nvme_multipath_opts {
	enum rpc_bdev_nvme_multipath_policy policy;
	enum rpc_bdev_nvme_multipath_selector selector;
	uint32_t min_io;
};


static const struct spdk_json_object_decoder rpc_bdev_nvme_multipath_opts_decoders[] = {
		{"policy", offsetof(struct rpc_bdev_nvme_multipath_opts, policy), rpc_decode_bdev_nvme_multipath_policy, true},
		{"selector", offsetof(struct rpc_bdev_nvme_multipath_opts, selector), rpc_decode_bdev_nvme_multipath_selector, true},
		{"min_io", offsetof(struct rpc_bdev_nvme_multipath_opts, min_io), spdk_json_decode_uint32, true},
};

static inline int
rpc_decode_bdev_nvme_multipath_opts(const struct spdk_json_val *val, void *out)
{
	return spdk_json_decode_object(val, rpc_bdev_nvme_multipath_opts_decoders,
				       SPDK_COUNTOF(rpc_bdev_nvme_multipath_opts_decoders), out);
}


static inline void
free_rpc_bdev_nvme_multipath_opts(struct rpc_bdev_nvme_multipath_opts *req)
{
}

struct rpc_iscsi_auth_secret {
	char * user;
	char * secret;
	char * muser;
	char * msecret;
};


static const struct spdk_json_object_decoder rpc_iscsi_auth_secret_decoders[] = {
		{"user", offsetof(struct rpc_iscsi_auth_secret, user), spdk_json_decode_string, false},
		{"secret", offsetof(struct rpc_iscsi_auth_secret, secret), spdk_json_decode_string, false},
		{"muser", offsetof(struct rpc_iscsi_auth_secret, muser), spdk_json_decode_string, true},
		{"msecret", offsetof(struct rpc_iscsi_auth_secret, msecret), spdk_json_decode_string, true},
};

static inline int
rpc_decode_iscsi_auth_secret(const struct spdk_json_val *val, void *out)
{
	return spdk_json_decode_object(val, rpc_iscsi_auth_secret_decoders,
				       SPDK_COUNTOF(rpc_iscsi_auth_secret_decoders), out);
}


static inline void
free_rpc_iscsi_auth_secret(struct rpc_iscsi_auth_secret *req)
{
	free(req->user);
	free(req->secret);
	free(req->muser);
	free(req->msecret);
}

struct rpc_iscsi_pg_ig_map {
	int32_t ig_tag;
	int32_t pg_tag;
};


static const struct spdk_json_object_decoder rpc_iscsi_pg_ig_map_decoders[] = {
		{"ig_tag", offsetof(struct rpc_iscsi_pg_ig_map, ig_tag), spdk_json_decode_int32, false},
		{"pg_tag", offsetof(struct rpc_iscsi_pg_ig_map, pg_tag), spdk_json_decode_int32, false},
};

static inline int
rpc_decode_iscsi_pg_ig_map(const struct spdk_json_val *val, void *out)
{
	return spdk_json_decode_object(val, rpc_iscsi_pg_ig_map_decoders,
				       SPDK_COUNTOF(rpc_iscsi_pg_ig_map_decoders), out);
}


static inline void
free_rpc_iscsi_pg_ig_map(struct rpc_iscsi_pg_ig_map *req)
{
}

struct rpc_iscsi_portal {
	char * host;
	char * port;
};


static const struct spdk_json_object_decoder rpc_iscsi_portal_decoders[] = {
		{"host", offsetof(struct rpc_iscsi_portal, host), spdk_json_decode_string, false},
		{"port", offsetof(struct rpc_iscsi_portal, port), spdk_json_decode_string, false},
};

static inline int
rpc_decode_iscsi_portal(const struct spdk_json_val *val, void *out)
{
	return spdk_json_decode_object(val, rpc_iscsi_portal_decoders,
				       SPDK_COUNTOF(rpc_iscsi_portal_decoders), out);
}


static inline void
free_rpc_iscsi_portal(struct rpc_iscsi_portal *req)
{
	free(req->host);
	free(req->port);
}

struct rpc_scsi_lun {
	int32_t lun_id;
	char * bdev_name;
};


static const struct spdk_json_object_decoder rpc_scsi_lun_decoders[] = {
		{"lun_id", offsetof(struct rpc_scsi_lun, lun_id), spdk_json_decode_int32, true},
		{"bdev_name", offsetof(struct rpc_scsi_lun, bdev_name), spdk_json_decode_string, true},
};

static inline int
rpc_decode_scsi_lun(const struct spdk_json_val *val, void *out)
{
	return spdk_json_decode_object(val, rpc_scsi_lun_decoders,
				       SPDK_COUNTOF(rpc_scsi_lun_decoders), out);
}


static inline void
free_rpc_scsi_lun(struct rpc_scsi_lun *req)
{
	free(req->bdev_name);
}

struct rpc_nvmf_listen_address {
	char * trtype;
	char * adrfam;
	char * traddr;
	char * trsvcid;
};


static const struct spdk_json_object_decoder rpc_nvmf_listen_address_decoders[] = {
		{"trtype", offsetof(struct rpc_nvmf_listen_address, trtype), spdk_json_decode_string, false},
		{"adrfam", offsetof(struct rpc_nvmf_listen_address, adrfam), spdk_json_decode_string, true},
		{"traddr", offsetof(struct rpc_nvmf_listen_address, traddr), spdk_json_decode_string, false},
		{"trsvcid", offsetof(struct rpc_nvmf_listen_address, trsvcid), spdk_json_decode_string, true},
};

static inline int
rpc_decode_nvmf_listen_address(const struct spdk_json_val *val, void *out)
{
	return spdk_json_decode_object(val, rpc_nvmf_listen_address_decoders,
				       SPDK_COUNTOF(rpc_nvmf_listen_address_decoders), out);
}


static inline void
free_rpc_nvmf_listen_address(struct rpc_nvmf_listen_address *req)
{
	free(req->trtype);
	free(req->adrfam);
	free(req->traddr);
	free(req->trsvcid);
}

struct rpc_nvmf_namespace {
	uint32_t nsid;
	char * bdev_name;
	char nguid[16];
	char eui64[8];
	struct spdk_uuid uuid;
	char * ptpl_file;
	uint32_t anagrpid;
	bool no_auto_visible;
	bool hide_metadata;
};



static inline void
free_rpc_nvmf_namespace(struct rpc_nvmf_namespace *req)
{
	free(req->bdev_name);
	free(req->ptpl_file);
}

struct rpc_nvmf_admin_cmd_passthru {
	bool identify_ctrlr;
	bool identify_uuid_list;
	bool get_log_page;
	bool get_set_features;
	bool sanitize;
	bool security_send_recv;
	bool fw_update;
	bool nvme_mi;
	bool vendor_specific;
};


static const struct spdk_json_object_decoder rpc_nvmf_admin_cmd_passthru_decoders[] = {
		{"identify_ctrlr", offsetof(struct rpc_nvmf_admin_cmd_passthru, identify_ctrlr), spdk_json_decode_bool, true},
		{"identify_uuid_list", offsetof(struct rpc_nvmf_admin_cmd_passthru, identify_uuid_list), spdk_json_decode_bool, true},
		{"get_log_page", offsetof(struct rpc_nvmf_admin_cmd_passthru, get_log_page), spdk_json_decode_bool, true},
		{"get_set_features", offsetof(struct rpc_nvmf_admin_cmd_passthru, get_set_features), spdk_json_decode_bool, true},
		{"sanitize", offsetof(struct rpc_nvmf_admin_cmd_passthru, sanitize), spdk_json_decode_bool, true},
		{"security_send_recv", offsetof(struct rpc_nvmf_admin_cmd_passthru, security_send_recv), spdk_json_decode_bool, true},
		{"fw_update", offsetof(struct rpc_nvmf_admin_cmd_passthru, fw_update), spdk_json_decode_bool, true},
		{"nvme_mi", offsetof(struct rpc_nvmf_admin_cmd_passthru, nvme_mi), spdk_json_decode_bool, true},
		{"vendor_specific", offsetof(struct rpc_nvmf_admin_cmd_passthru, vendor_specific), spdk_json_decode_bool, true},
};

static inline int
rpc_decode_nvmf_admin_cmd_passthru(const struct spdk_json_val *val, void *out)
{
	return spdk_json_decode_object(val, rpc_nvmf_admin_cmd_passthru_decoders,
				       SPDK_COUNTOF(rpc_nvmf_admin_cmd_passthru_decoders), out);
}


static inline void
free_rpc_nvmf_admin_cmd_passthru(struct rpc_nvmf_admin_cmd_passthru *req)
{
}

struct rpc_vhost_blk_controller_params {
	char * bdev;
	bool readonly;
};


static const struct spdk_json_object_decoder rpc_vhost_blk_controller_params_decoders[] = {
		{"bdev", offsetof(struct rpc_vhost_blk_controller_params, bdev), spdk_json_decode_string, true},
		{"readonly", offsetof(struct rpc_vhost_blk_controller_params, readonly), spdk_json_decode_bool, true},
};

static inline int
rpc_decode_vhost_blk_controller_params(const struct spdk_json_val *val, void *out)
{
	return spdk_json_decode_object(val, rpc_vhost_blk_controller_params_decoders,
				       SPDK_COUNTOF(rpc_vhost_blk_controller_params_decoders), out);
}


static inline void
free_rpc_vhost_blk_controller_params(struct rpc_vhost_blk_controller_params *req)
{
	free(req->bdev);
}

struct rpc_vhost_scsi_controller_params {
	char * target_name;
	uint32_t id;
	uint32_t scsi_dev_num;
	struct rpc_scsi_luns *luns;
};


static const struct spdk_json_object_decoder rpc_vhost_scsi_controller_params_decoders[] = {
		{"target_name", offsetof(struct rpc_vhost_scsi_controller_params, target_name), spdk_json_decode_string, true},
		{"id", offsetof(struct rpc_vhost_scsi_controller_params, id), spdk_json_decode_uint32, true},
		{"scsi_dev_num", offsetof(struct rpc_vhost_scsi_controller_params, scsi_dev_num), spdk_json_decode_uint32, true},
};

static inline int
rpc_decode_vhost_scsi_controller_params(const struct spdk_json_val *val, void *out)
{
	return spdk_json_decode_object(val, rpc_vhost_scsi_controller_params_decoders,
				       SPDK_COUNTOF(rpc_vhost_scsi_controller_params_decoders), out);
}


static inline void
free_rpc_vhost_scsi_controller_params(struct rpc_vhost_scsi_controller_params *req)
{
	free(req->target_name);
}


/* Array definitions section */


#define RPC_BDEV_HISTOGRAM_BORDERS_MAX 256

struct rpc_bdev_histogram_borders {
	size_t    count;
	uint64_t items[RPC_BDEV_HISTOGRAM_BORDERS_MAX];
};

static inline int
rpc_decode_bdev_histogram_borders(const struct spdk_json_val *val, void *out)
{
	struct rpc_bdev_histogram_borders *arr = out;

	return spdk_json_decode_array(val, spdk_json_decode_uint64, arr->items,
				      RPC_BDEV_HISTOGRAM_BORDERS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}



static inline void
free_rpc_bdev_histogram_borders(struct rpc_bdev_histogram_borders *arr)
{
}

#define RPC_BDEV_IOSTAT_NAMES_MAX 1024

struct rpc_bdev_iostat_names {
	size_t    count;
	char      *items[RPC_BDEV_IOSTAT_NAMES_MAX];
};

static inline int
rpc_decode_bdev_iostat_names(const struct spdk_json_val *val, void *out)
{
	struct rpc_bdev_iostat_names *arr = out;

	return spdk_json_decode_array(val, spdk_json_decode_string, arr->items,
				      RPC_BDEV_IOSTAT_NAMES_MAX, &arr->count,
				      sizeof(arr->items[0]));
}



static inline void
free_rpc_bdev_iostat_names(struct rpc_bdev_iostat_names *arr)
{
	size_t i;

	for (i = 0; i < arr->count; i++) {
		free(arr->items[i]);
	}
}

#define RPC_ISCSI_INITIATORS_MAX 256

struct rpc_iscsi_initiators {
	size_t    count;
	char      *items[RPC_ISCSI_INITIATORS_MAX];
};

static inline int
rpc_decode_iscsi_initiators(const struct spdk_json_val *val, void *out)
{
	struct rpc_iscsi_initiators *arr = out;

	return spdk_json_decode_array(val, spdk_json_decode_string, arr->items,
				      RPC_ISCSI_INITIATORS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}



static inline void
free_rpc_iscsi_initiators(struct rpc_iscsi_initiators *arr)
{
	size_t i;

	for (i = 0; i < arr->count; i++) {
		free(arr->items[i]);
	}
}

#define RPC_ISCSI_NETMASKS_MAX 256

struct rpc_iscsi_netmasks {
	size_t    count;
	char      *items[RPC_ISCSI_NETMASKS_MAX];
};

static inline int
rpc_decode_iscsi_netmasks(const struct spdk_json_val *val, void *out)
{
	struct rpc_iscsi_netmasks *arr = out;

	return spdk_json_decode_array(val, spdk_json_decode_string, arr->items,
				      RPC_ISCSI_NETMASKS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}



static inline void
free_rpc_iscsi_netmasks(struct rpc_iscsi_netmasks *arr)
{
	size_t i;

	for (i = 0; i < arr->count; i++) {
		free(arr->items[i]);
	}
}

#define RPC_ISCSI_PG_IG_MAPS_MAX 256

struct rpc_iscsi_pg_ig_maps {
	size_t    count;
	struct rpc_iscsi_pg_ig_map  items[RPC_ISCSI_PG_IG_MAPS_MAX];
};

static inline int
rpc_decode_iscsi_pg_ig_maps(const struct spdk_json_val *val, void *out)
{
	struct rpc_iscsi_pg_ig_maps *arr = out;

	return spdk_json_decode_array(val, rpc_decode_iscsi_pg_ig_map, arr->items,
				      RPC_ISCSI_PG_IG_MAPS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}



static inline void
free_rpc_iscsi_pg_ig_maps(struct rpc_iscsi_pg_ig_maps *arr)
{
	size_t i;

	for (i = 0; i < arr->count; i++) {
		free_rpc_iscsi_pg_ig_map(&arr->items[i]);
	}
}

#define RPC_SCSI_LUNS_MAX 64

struct rpc_scsi_luns {
	size_t    count;
	struct rpc_scsi_lun  items[RPC_SCSI_LUNS_MAX];
};

static inline int
rpc_decode_scsi_luns(const struct spdk_json_val *val, void *out)
{
	struct rpc_scsi_luns *arr = out;

	return spdk_json_decode_array(val, rpc_decode_scsi_lun, arr->items,
				      RPC_SCSI_LUNS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}



static inline void
free_rpc_scsi_luns(struct rpc_scsi_luns *arr)
{
	size_t i;

	for (i = 0; i < arr->count; i++) {
		free_rpc_scsi_lun(&arr->items[i]);
	}
}

#define RPC_ISCSI_PORTALS_MAX 1024

struct rpc_iscsi_portals {
	size_t    count;
	struct rpc_iscsi_portal  items[RPC_ISCSI_PORTALS_MAX];
};

static inline int
rpc_decode_iscsi_portals(const struct spdk_json_val *val, void *out)
{
	struct rpc_iscsi_portals *arr = out;

	return spdk_json_decode_array(val, rpc_decode_iscsi_portal, arr->items,
				      RPC_ISCSI_PORTALS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}



static inline void
free_rpc_iscsi_portals(struct rpc_iscsi_portals *arr)
{
	size_t i;

	for (i = 0; i < arr->count; i++) {
		free_rpc_iscsi_portal(&arr->items[i]);
	}
}

#define RPC_ISCSI_AUTH_SECRETS_MAX 64

struct rpc_iscsi_auth_secrets {
	size_t    count;
	struct rpc_iscsi_auth_secret  items[RPC_ISCSI_AUTH_SECRETS_MAX];
};

static inline int
rpc_decode_iscsi_auth_secrets(const struct spdk_json_val *val, void *out)
{
	struct rpc_iscsi_auth_secrets *arr = out;

	return spdk_json_decode_array(val, rpc_decode_iscsi_auth_secret, arr->items,
				      RPC_ISCSI_AUTH_SECRETS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}



static inline void
free_rpc_iscsi_auth_secrets(struct rpc_iscsi_auth_secrets *arr)
{
	size_t i;

	for (i = 0; i < arr->count; i++) {
		free_rpc_iscsi_auth_secret(&arr->items[i]);
	}
}

#define RPC_DHCHAP_DIGESTS_MAX 32

struct rpc_dhchap_digests {
	size_t    count;
	enum rpc_dhchap_digest  items[RPC_DHCHAP_DIGESTS_MAX];
};

static inline int
rpc_decode_dhchap_digests(const struct spdk_json_val *val, void *out)
{
	struct rpc_dhchap_digests *arr = out;

	return spdk_json_decode_array(val, rpc_decode_dhchap_digest, arr->items,
				      RPC_DHCHAP_DIGESTS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}


static inline int
rpc_decode_dhchap_digests_bitmask(const struct spdk_json_val *val, void *out)
{
	uint32_t *flags = out;
	uint32_t mask = 0;
	struct rpc_dhchap_digests array = {};
	size_t i;
	int rc;

	rc = rpc_decode_dhchap_digests(val, &array);
	if (rc != 0) {
		return rc;
	}

	for (i = 0; i < array.count; i++) {
		mask |= SPDK_BIT(array.items[i]);
	}
	*flags = mask;

	return 0;
}


static inline void
free_rpc_dhchap_digests(struct rpc_dhchap_digests *arr)
{
}

#define RPC_DHCHAP_DHGROUPS_MAX 32

struct rpc_dhchap_dhgroups {
	size_t    count;
	enum rpc_dhchap_dhgroup  items[RPC_DHCHAP_DHGROUPS_MAX];
};

static inline int
rpc_decode_dhchap_dhgroups(const struct spdk_json_val *val, void *out)
{
	struct rpc_dhchap_dhgroups *arr = out;

	return spdk_json_decode_array(val, rpc_decode_dhchap_dhgroup, arr->items,
				      RPC_DHCHAP_DHGROUPS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}


static inline int
rpc_decode_dhchap_dhgroups_bitmask(const struct spdk_json_val *val, void *out)
{
	uint32_t *flags = out;
	uint32_t mask = 0;
	struct rpc_dhchap_dhgroups array = {};
	size_t i;
	int rc;

	rc = rpc_decode_dhchap_dhgroups(val, &array);
	if (rc != 0) {
		return rc;
	}

	for (i = 0; i < array.count; i++) {
		mask |= SPDK_BIT(array.items[i]);
	}
	*flags = mask;

	return 0;
}


static inline void
free_rpc_dhchap_dhgroups(struct rpc_dhchap_dhgroups *arr)
{
}

#define RPC_ONCS_FEATURES_MAX 16

struct rpc_oncs_features {
	size_t    count;
	enum rpc_oncs_feature  items[RPC_ONCS_FEATURES_MAX];
};

static inline int
rpc_decode_oncs_features(const struct spdk_json_val *val, void *out)
{
	struct rpc_oncs_features *arr = out;

	return spdk_json_decode_array(val, rpc_decode_oncs_feature, arr->items,
				      RPC_ONCS_FEATURES_MAX, &arr->count,
				      sizeof(arr->items[0]));
}


static inline int
rpc_decode_oncs_features_bitmask(const struct spdk_json_val *val, void *out)
{
	uint16_t *flags = out;
	uint16_t mask = 0;
	struct rpc_oncs_features array = {};
	size_t i;
	int rc;

	rc = rpc_decode_oncs_features(val, &array);
	if (rc != 0) {
		return rc;
	}

	for (i = 0; i < array.count; i++) {
		mask |= SPDK_BIT(array.items[i]);
	}
	*flags &= ~mask;

	return 0;
}


static inline void
free_rpc_oncs_features(struct rpc_oncs_features *arr)
{
}

#define RPC_FUSES_FEATURES_MAX 16

struct rpc_fuses_features {
	size_t    count;
	enum rpc_fuses_feature  items[RPC_FUSES_FEATURES_MAX];
};

static inline int
rpc_decode_fuses_features(const struct spdk_json_val *val, void *out)
{
	struct rpc_fuses_features *arr = out;

	return spdk_json_decode_array(val, rpc_decode_fuses_feature, arr->items,
				      RPC_FUSES_FEATURES_MAX, &arr->count,
				      sizeof(arr->items[0]));
}


static inline int
rpc_decode_fuses_features_bitmask(const struct spdk_json_val *val, void *out)
{
	uint16_t *flags = out;
	uint16_t mask = 0;
	struct rpc_fuses_features array = {};
	size_t i;
	int rc;

	rc = rpc_decode_fuses_features(val, &array);
	if (rc != 0) {
		return rc;
	}

	for (i = 0; i < array.count; i++) {
		mask |= SPDK_BIT(array.items[i]);
	}
	*flags &= ~mask;

	return 0;
}


static inline void
free_rpc_fuses_features(struct rpc_fuses_features *arr)
{
}

#define RPC_RAID_BASE_BDEVS_MAX 255

struct rpc_raid_base_bdevs {
	size_t    count;
	char      *items[RPC_RAID_BASE_BDEVS_MAX];
};

static inline int
rpc_decode_raid_base_bdevs(const struct spdk_json_val *val, void *out)
{
	struct rpc_raid_base_bdevs *arr = out;

	return spdk_json_decode_array(val, spdk_json_decode_string, arr->items,
				      RPC_RAID_BASE_BDEVS_MAX, &arr->count,
				      sizeof(arr->items[0]));
}



static inline void
free_rpc_raid_base_bdevs(struct rpc_raid_base_bdevs *arr)
{
	size_t i;

	for (i = 0; i < arr->count; i++) {
		free(arr->items[i]);
	}
}


/* Deprecated-parameter decoder helpers section
 *
 * For each method parameter marked `"deprecated": true` in the schema,
 * emit a `static inline` decoder that logs a deprecation warning before
 * delegating to the normal type decoder. The matching
 * SPDK_LOG_DEPRECATION_REGISTER (defining `_deprecated_<tag>`) must live
 * in the consuming .c file so that each tag is registered exactly once.
 *
 * Because this header is pulled in by every TU that includes
 * <spdk/rpc.h>, forward-declare each `_deprecated_<tag>` symbol as
 * extern so the static-inline bodies parse cleanly even in TUs that do
 * not own the REGISTER macro. Unused static-inline functions are not
 * emitted, so only TUs that actually call a decoder require the symbol
 * at link time.
 */


extern struct spdk_deprecation *_deprecated_nvmf_create_transport_io_unit_size;
extern struct spdk_deprecation *_deprecated_nvmf_create_transport_num_shared_buffers;
extern struct spdk_deprecation *_deprecated_nvmf_create_transport_buf_cache_size;
extern struct spdk_deprecation *_deprecated_nvmf_create_subsystem_max_discard_size_kib;
extern struct spdk_deprecation *_deprecated_nvmf_create_subsystem_max_write_zeroes_size_kib;


static inline int
rpc_decode_io_unit_size(const struct spdk_json_val *val, void *out)
{
	SPDK_LOG_DEPRECATED(nvmf_create_transport_io_unit_size);
	return spdk_json_decode_uint32(val, out);
}

static inline int
rpc_decode_num_shared_buffers(const struct spdk_json_val *val, void *out)
{
	SPDK_LOG_DEPRECATED(nvmf_create_transport_num_shared_buffers);
	return spdk_json_decode_uint32(val, out);
}

static inline int
rpc_decode_buf_cache_size(const struct spdk_json_val *val, void *out)
{
	SPDK_LOG_DEPRECATED(nvmf_create_transport_buf_cache_size);
	return spdk_json_decode_uint32(val, out);
}

static inline int
rpc_decode_max_discard_size_kib(const struct spdk_json_val *val, void *out)
{
	SPDK_LOG_DEPRECATED(nvmf_create_subsystem_max_discard_size_kib);
	return spdk_json_decode_uint64(val, out);
}

static inline int
rpc_decode_max_write_zeroes_size_kib(const struct spdk_json_val *val, void *out)
{
	SPDK_LOG_DEPRECATED(nvmf_create_subsystem_max_write_zeroes_size_kib);
	return spdk_json_decode_uint64(val, out);
}


/* RPC objects section */


struct rpc_spdk_kill_instance_ctx {
	enum rpc_kill_signal sig_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_spdk_kill_instance(struct rpc_spdk_kill_instance_ctx *req)
{
}

struct rpc_framework_monitor_context_switch_ctx {
	bool enabled;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_framework_monitor_context_switch(struct rpc_framework_monitor_context_switch_ctx *req)
{
}



struct rpc_rpc_get_methods_ctx {
	bool current;
	bool include_aliases;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_rpc_get_methods(struct rpc_rpc_get_methods_ctx *req)
{
}


struct rpc_framework_get_config_ctx {
	char * name;
	bool with_batches;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_framework_get_config(struct rpc_framework_get_config_ctx *req)
{
	free(req->name);
}


struct rpc_framework_set_scheduler_ctx {
	char * name;
	uint64_t period;
	uint8_t load_limit;
	uint8_t core_limit;
	uint8_t core_busy;
	char * mappings;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_framework_set_scheduler(struct rpc_framework_set_scheduler_ctx *req)
{
	free(req->name);
	free(req->mappings);
}



struct rpc_scheduler_set_options_ctx {
	uint32_t scheduling_core;
	char * isolated_core_mask;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_scheduler_set_options(struct rpc_scheduler_set_options_ctx *req)
{
	free(req->isolated_core_mask);
}




struct rpc_thread_set_cpumask_ctx {
	uint64_t id;
	char * cpumask;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_thread_set_cpumask(struct rpc_thread_set_cpumask_ctx *req)
{
	free(req->cpumask);
}

struct rpc_trace_enable_tpoint_group_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_trace_enable_tpoint_group(struct rpc_trace_enable_tpoint_group_ctx *req)
{
	free(req->name);
}

struct rpc_trace_disable_tpoint_group_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_trace_disable_tpoint_group(struct rpc_trace_disable_tpoint_group_ctx *req)
{
	free(req->name);
}

struct rpc_trace_set_tpoint_mask_ctx {
	char * name;
	uint64_t tpoint_mask;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_trace_set_tpoint_mask(struct rpc_trace_set_tpoint_mask_ctx *req)
{
	free(req->name);
}

struct rpc_trace_clear_tpoint_mask_ctx {
	char * name;
	uint64_t tpoint_mask;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_trace_clear_tpoint_mask(struct rpc_trace_clear_tpoint_mask_ctx *req)
{
	free(req->name);
}




struct rpc_log_set_print_level_ctx {
	enum rpc_log_level level;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_log_set_print_level(struct rpc_log_set_print_level_ctx *req)
{
}


struct rpc_log_set_level_ctx {
	enum rpc_log_level level;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_log_set_level(struct rpc_log_set_level_ctx *req)
{
}


struct rpc_log_set_flag_ctx {
	char * flag;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_log_set_flag(struct rpc_log_set_flag_ctx *req)
{
	free(req->flag);
}

struct rpc_log_clear_flag_ctx {
	char * flag;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_log_clear_flag(struct rpc_log_clear_flag_ctx *req)
{
	free(req->flag);
}


struct rpc_log_enable_timestamps_ctx {
	bool enabled;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_log_enable_timestamps(struct rpc_log_enable_timestamps_ctx *req)
{
}






struct rpc_accel_assign_opc_ctx {
	char * opname;
	char * module;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_accel_assign_opc(struct rpc_accel_assign_opc_ctx *req)
{
	free(req->opname);
	free(req->module);
}

struct rpc_accel_crypto_key_create_ctx {
	enum rpc_accel_crypto_cipher cipher;
	char * key;
	char * key2;
	enum rpc_accel_crypto_tweak_mode tweak_mode;
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_accel_crypto_key_create(struct rpc_accel_crypto_key_create_ctx *req)
{
	free(req->key);
	free(req->key2);
	free(req->name);
}

struct rpc_accel_crypto_key_destroy_ctx {
	char * key_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_accel_crypto_key_destroy(struct rpc_accel_crypto_key_destroy_ctx *req)
{
	free(req->key_name);
}

struct rpc_accel_crypto_keys_get_ctx {
	char * key_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_accel_crypto_keys_get(struct rpc_accel_crypto_keys_get_ctx *req)
{
	free(req->key_name);
}

struct rpc_accel_set_driver_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_accel_set_driver(struct rpc_accel_set_driver_ctx *req)
{
	free(req->name);
}

struct rpc_accel_set_options_ctx {
	uint32_t small_cache_size;
	uint32_t large_cache_size;
	uint32_t task_count;
	uint32_t sequence_count;
	uint32_t buf_count;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_accel_set_options(struct rpc_accel_set_options_ctx *req)
{
}


struct rpc_accel_error_inject_error_ctx {
	enum rpc_accel_error_opcode opcode;
	enum rpc_accel_error_inject_type type;
	uint64_t count;
	uint64_t interval;
	int32_t errcode;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_accel_error_inject_error(struct rpc_accel_error_inject_error_ctx *req)
{
}

struct rpc_compressdev_scan_accel_module_ctx {
	uint32_t pmd;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_compressdev_scan_accel_module(struct rpc_compressdev_scan_accel_module_ctx *req)
{
}

struct rpc_dsa_scan_accel_module_ctx {
	bool config_kernel_mode;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_dsa_scan_accel_module(struct rpc_dsa_scan_accel_module_ctx *req)
{
}





struct rpc_dpdk_cryptodev_set_driver_ctx {
	enum rpc_dpdk_cryptodev_driver driver_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_dpdk_cryptodev_set_driver(struct rpc_dpdk_cryptodev_set_driver_ctx *req)
{
}


struct rpc_mlx5_scan_accel_module_ctx {
	uint16_t qp_size;
	uint32_t num_requests;
	char * allowed_devs;
	uint16_t crypto_split_blocks;
	bool enable_driver;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_mlx5_scan_accel_module(struct rpc_mlx5_scan_accel_module_ctx *req)
{
	free(req->allowed_devs);
}

struct rpc_accel_mlx5_dump_stats_ctx {
	enum rpc_accel_mlx5_dump_state_level level;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_accel_mlx5_dump_stats(struct rpc_accel_mlx5_dump_stats_ctx *req)
{
}


struct rpc_bdev_set_options_ctx {
	uint32_t bdev_io_pool_size;
	uint32_t bdev_io_cache_size;
	bool bdev_auto_examine;
	uint32_t iobuf_small_cache_size;
	uint32_t iobuf_large_cache_size;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_set_options(struct rpc_bdev_set_options_ctx *req)
{
}

struct rpc_bdev_get_bdevs_ctx {
	char * name;
	uint64_t timeout;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_get_bdevs(struct rpc_bdev_get_bdevs_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_examine_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_examine(struct rpc_bdev_examine_ctx *req)
{
	free(req->name);
}


struct rpc_bdev_get_iostat_ctx {
	struct rpc_bdev_iostat_names names;
	bool per_channel;
	enum rpc_bdev_reset_stat_mode reset_mode;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_get_iostat(struct rpc_bdev_get_iostat_ctx *req)
{
	free_rpc_bdev_iostat_names(&req->names);
}

struct rpc_bdev_reset_iostat_ctx {
	char * name;
	enum rpc_bdev_reset_stat_mode mode;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_reset_iostat(struct rpc_bdev_reset_iostat_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_enable_histogram_ctx {
	char * name;
	bool enable;
	char * opc;
	uint8_t granularity;
	uint64_t min_nsec;
	uint64_t max_nsec;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_enable_histogram(struct rpc_bdev_enable_histogram_ctx *req)
{
	free(req->name);
	free(req->opc);
}

struct rpc_bdev_get_histogram_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_get_histogram(struct rpc_bdev_get_histogram_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_get_histogram_borders_ctx {
	char * name;
	struct rpc_bdev_histogram_borders borders;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_get_histogram_borders(struct rpc_bdev_get_histogram_borders_ctx *req)
{
	free(req->name);
	free_rpc_bdev_histogram_borders(&req->borders);
}

struct rpc_bdev_set_qos_limit_ctx {
	char * name;
	uint64_t rw_ios_per_sec;
	uint64_t rw_mbytes_per_sec;
	uint64_t r_mbytes_per_sec;
	uint64_t w_mbytes_per_sec;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_set_qos_limit(struct rpc_bdev_set_qos_limit_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_set_qd_sampling_period_ctx {
	char * name;
	uint64_t period;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_set_qd_sampling_period(struct rpc_bdev_set_qd_sampling_period_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_crypto_create_ctx {
	char * base_bdev_name;
	char * name;
	char * crypto_pmd;
	char * key;
	char * cipher;
	char * key2;
	char * key_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_crypto_create(struct rpc_bdev_crypto_create_ctx *req)
{
	free(req->base_bdev_name);
	free(req->name);
	free(req->crypto_pmd);
	free(req->key);
	free(req->cipher);
	free(req->key2);
	free(req->key_name);
}

struct rpc_bdev_crypto_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_crypto_delete(struct rpc_bdev_crypto_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ocf_create_ctx {
	char * name;
	char * mode;
	uint64_t cache_line_size;
	char * cache_bdev_name;
	char * core_bdev_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ocf_create(struct rpc_bdev_ocf_create_ctx *req)
{
	free(req->name);
	free(req->mode);
	free(req->cache_bdev_name);
	free(req->core_bdev_name);
}

struct rpc_bdev_ocf_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ocf_delete(struct rpc_bdev_ocf_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ocf_get_stats_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ocf_get_stats(struct rpc_bdev_ocf_get_stats_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ocf_reset_stats_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ocf_reset_stats(struct rpc_bdev_ocf_reset_stats_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ocf_get_bdevs_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ocf_get_bdevs(struct rpc_bdev_ocf_get_bdevs_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ocf_set_cache_mode_ctx {
	char * name;
	char * mode;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ocf_set_cache_mode(struct rpc_bdev_ocf_set_cache_mode_ctx *req)
{
	free(req->name);
	free(req->mode);
}

struct rpc_bdev_ocf_set_seqcutoff_ctx {
	char * name;
	char * policy;
	uint32_t threshold;
	uint32_t promotion_count;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ocf_set_seqcutoff(struct rpc_bdev_ocf_set_seqcutoff_ctx *req)
{
	free(req->name);
	free(req->policy);
}

struct rpc_bdev_ocf_flush_start_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ocf_flush_start(struct rpc_bdev_ocf_flush_start_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ocf_flush_status_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ocf_flush_status(struct rpc_bdev_ocf_flush_status_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_malloc_create_ctx {
	char * name;
	uint32_t block_size;
	uint64_t num_blocks;
	struct spdk_uuid uuid;
	uint32_t optimal_io_boundary;
	uint32_t md_size;
	bool md_interleave;
	int32_t dif_type;
	bool dif_is_head_of_md;
	uint32_t physical_block_size;
	uint32_t dif_pi_format;
	int32_t numa_id;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_malloc_create(struct rpc_bdev_malloc_create_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_malloc_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_malloc_delete(struct rpc_bdev_malloc_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_null_create_ctx {
	char * name;
	uint32_t block_size;
	uint64_t num_blocks;
	struct spdk_uuid uuid;
	uint32_t physical_block_size;
	uint32_t md_size;
	int32_t dif_type;
	uint32_t dif_pi_format;
	bool dif_is_head_of_md;
	uint32_t preferred_write_granularity;
	uint32_t preferred_write_alignment;
	uint32_t optimal_write_size;
	uint32_t preferred_unmap_granularity;
	uint32_t preferred_unmap_alignment;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_null_create(struct rpc_bdev_null_create_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_null_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_null_delete(struct rpc_bdev_null_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_null_resize_ctx {
	char * name;
	uint64_t new_size;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_null_resize(struct rpc_bdev_null_resize_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_aio_create_ctx {
	char * name;
	char * filename;
	uint32_t block_size;
	bool readonly;
	bool fallocate;
	struct spdk_uuid uuid;
	bool nowait;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_aio_create(struct rpc_bdev_aio_create_ctx *req)
{
	free(req->name);
	free(req->filename);
}

struct rpc_bdev_aio_rescan_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_aio_rescan(struct rpc_bdev_aio_rescan_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_aio_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_aio_delete(struct rpc_bdev_aio_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_set_options_ctx {
	enum rpc_bdev_nvme_timeout_action action_on_timeout;
	uint64_t timeout_us;
	uint64_t timeout_admin_us;
	uint32_t keep_alive_timeout_ms;
	uint32_t arbitration_burst;
	uint32_t low_priority_weight;
	uint32_t medium_priority_weight;
	uint32_t high_priority_weight;
	uint64_t nvme_adminq_poll_period_us;
	uint64_t nvme_ioq_poll_period_us;
	uint32_t io_queue_requests;
	bool delay_cmd_submit;
	uint32_t transport_retry_count;
	int32_t bdev_retry_count;
	uint8_t transport_ack_timeout;
	int32_t ctrlr_loss_timeout_sec;
	uint32_t reconnect_delay_sec;
	uint32_t fast_io_fail_timeout_sec;
	bool disable_auto_failback;
	bool generate_uuids;
	uint8_t transport_tos;
	bool nvme_error_stat;
	uint32_t rdma_srq_size;
	bool io_path_stat;
	bool allow_accel_sequence;
	uint32_t rdma_max_cq_size;
	uint16_t rdma_cm_event_timeout_ms;
	struct rpc_dhchap_digests dhchap_digests;
	struct rpc_dhchap_dhgroups dhchap_dhgroups;
	bool rdma_umr_per_io;
	uint32_t tcp_connect_timeout_ms;
	bool enable_flush;
	struct rpc_bdev_nvme_multipath_opts multipath_opts;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_set_options(struct rpc_bdev_nvme_set_options_ctx *req)
{
	free_rpc_dhchap_digests(&req->dhchap_digests);
	free_rpc_dhchap_dhgroups(&req->dhchap_dhgroups);
	free_rpc_bdev_nvme_multipath_opts(&req->multipath_opts);
}

struct rpc_bdev_nvme_set_hotplug_ctx {
	bool enable;
	uint64_t period_us;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_set_hotplug(struct rpc_bdev_nvme_set_hotplug_ctx *req)
{
}

struct rpc_bdev_nvme_attach_controller_ctx {
	char * name;
	char * trtype;
	char * traddr;
	char * adrfam;
	char * trsvcid;
	char * priority;
	char * subnqn;
	char * hostnqn;
	char * hostaddr;
	char * hostsvcid;
	bool prchk_reftag;
	bool prchk_guard;
	bool hdgst;
	bool ddgst;
	uint64_t fabrics_connect_timeout_us;
	enum rpc_bdev_nvme_multipath_mode multipath;
	uint32_t num_io_queues;
	int32_t ctrlr_loss_timeout_sec;
	uint32_t reconnect_delay_sec;
	uint32_t fast_io_fail_timeout_sec;
	char * psk;
	uint32_t max_bdevs;
	char * dhchap_key;
	char * dhchap_ctrlr_key;
	bool allow_unrecognized_csi;
	struct rpc_bdev_nvme_multipath_opts multipath_opts;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_attach_controller(struct rpc_bdev_nvme_attach_controller_ctx *req)
{
	free(req->name);
	free(req->trtype);
	free(req->traddr);
	free(req->adrfam);
	free(req->trsvcid);
	free(req->priority);
	free(req->subnqn);
	free(req->hostnqn);
	free(req->hostaddr);
	free(req->hostsvcid);
	free(req->psk);
	free(req->dhchap_key);
	free(req->dhchap_ctrlr_key);
	free_rpc_bdev_nvme_multipath_opts(&req->multipath_opts);
}

struct rpc_bdev_nvme_get_controllers_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_get_controllers(struct rpc_bdev_nvme_get_controllers_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_detach_controller_ctx {
	char * name;
	char * trtype;
	char * traddr;
	char * adrfam;
	char * trsvcid;
	char * subnqn;
	char * hostaddr;
	char * hostsvcid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_detach_controller(struct rpc_bdev_nvme_detach_controller_ctx *req)
{
	free(req->name);
	free(req->trtype);
	free(req->traddr);
	free(req->adrfam);
	free(req->trsvcid);
	free(req->subnqn);
	free(req->hostaddr);
	free(req->hostsvcid);
}

struct rpc_bdev_nvme_reset_controller_ctx {
	char * name;
	uint16_t cntlid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_reset_controller(struct rpc_bdev_nvme_reset_controller_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_enable_controller_ctx {
	char * name;
	uint16_t cntlid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_enable_controller(struct rpc_bdev_nvme_enable_controller_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_disable_controller_ctx {
	char * name;
	uint16_t cntlid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_disable_controller(struct rpc_bdev_nvme_disable_controller_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_start_discovery_ctx {
	char * name;
	char * trtype;
	char * traddr;
	char * adrfam;
	char * trsvcid;
	char * hostnqn;
	bool wait_for_attach;
	uint64_t attach_timeout_ms;
	int32_t ctrlr_loss_timeout_sec;
	uint32_t reconnect_delay_sec;
	uint32_t fast_io_fail_timeout_sec;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_start_discovery(struct rpc_bdev_nvme_start_discovery_ctx *req)
{
	free(req->name);
	free(req->trtype);
	free(req->traddr);
	free(req->adrfam);
	free(req->trsvcid);
	free(req->hostnqn);
}

struct rpc_bdev_nvme_stop_discovery_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_stop_discovery(struct rpc_bdev_nvme_stop_discovery_ctx *req)
{
	free(req->name);
}


struct rpc_bdev_nvme_get_io_paths_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_get_io_paths(struct rpc_bdev_nvme_get_io_paths_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_set_preferred_path_ctx {
	char * name;
	uint16_t cntlid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_set_preferred_path(struct rpc_bdev_nvme_set_preferred_path_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_set_multipath_policy_ctx {
	char * name;
	enum rpc_bdev_nvme_multipath_policy policy;
	enum rpc_bdev_nvme_multipath_selector selector;
	uint32_t rr_min_io;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_set_multipath_policy(struct rpc_bdev_nvme_set_multipath_policy_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_get_path_iostat_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_get_path_iostat(struct rpc_bdev_nvme_get_path_iostat_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_cuse_register_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_cuse_register(struct rpc_bdev_nvme_cuse_register_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_cuse_unregister_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_cuse_unregister(struct rpc_bdev_nvme_cuse_unregister_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_set_keys_ctx {
	char * name;
	char * dhchap_key;
	char * dhchap_ctrlr_key;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_set_keys(struct rpc_bdev_nvme_set_keys_ctx *req)
{
	free(req->name);
	free(req->dhchap_key);
	free(req->dhchap_ctrlr_key);
}

struct rpc_bdev_zone_block_create_ctx {
	char * name;
	char * base_bdev;
	uint64_t zone_capacity;
	uint64_t optimal_open_zones;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_zone_block_create(struct rpc_bdev_zone_block_create_ctx *req)
{
	free(req->name);
	free(req->base_bdev);
}

struct rpc_bdev_zone_block_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_zone_block_delete(struct rpc_bdev_zone_block_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_apply_firmware_ctx {
	char * filename;
	char * bdev_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_apply_firmware(struct rpc_bdev_nvme_apply_firmware_ctx *req)
{
	free(req->filename);
	free(req->bdev_name);
}


struct rpc_bdev_nvme_get_controller_health_info_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_get_controller_health_info(struct rpc_bdev_nvme_get_controller_health_info_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_rbd_register_cluster_ctx {
	char * name;
	char * user_id;
	char * config_param;
	char * config_file;
	char * key_file;
	char * core_mask;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_rbd_register_cluster(struct rpc_bdev_rbd_register_cluster_ctx *req)
{
	free(req->name);
	free(req->user_id);
	free(req->config_param);
	free(req->config_file);
	free(req->key_file);
	free(req->core_mask);
}

struct rpc_bdev_rbd_unregister_cluster_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_rbd_unregister_cluster(struct rpc_bdev_rbd_unregister_cluster_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_rbd_get_clusters_info_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_rbd_get_clusters_info(struct rpc_bdev_rbd_get_clusters_info_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_rbd_create_ctx {
	char * name;
	char * user_id;
	char * pool_name;
	char * rbd_name;
	uint32_t block_size;
	char * config;
	char * cluster_name;
	struct spdk_uuid uuid;
	bool read_only;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_rbd_create(struct rpc_bdev_rbd_create_ctx *req)
{
	free(req->name);
	free(req->user_id);
	free(req->pool_name);
	free(req->rbd_name);
	free(req->config);
	free(req->cluster_name);
}

struct rpc_bdev_rbd_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_rbd_delete(struct rpc_bdev_rbd_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_rbd_resize_ctx {
	char * name;
	uint64_t new_size;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_rbd_resize(struct rpc_bdev_rbd_resize_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_delay_create_ctx {
	char * name;
	char * base_bdev_name;
	uint64_t avg_read_latency;
	uint64_t p99_read_latency;
	uint64_t avg_write_latency;
	uint64_t p99_write_latency;
	struct spdk_uuid uuid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_delay_create(struct rpc_bdev_delay_create_ctx *req)
{
	free(req->name);
	free(req->base_bdev_name);
}

struct rpc_bdev_delay_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_delay_delete(struct rpc_bdev_delay_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_delay_update_latency_ctx {
	char * delay_bdev_name;
	enum rpc_bdev_delay_latency_type latency_type;
	uint64_t latency_us;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_delay_update_latency(struct rpc_bdev_delay_update_latency_ctx *req)
{
	free(req->delay_bdev_name);
}

struct rpc_bdev_error_create_ctx {
	char * base_name;
	struct spdk_uuid uuid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_error_create(struct rpc_bdev_error_create_ctx *req)
{
	free(req->base_name);
}

struct rpc_bdev_error_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_error_delete(struct rpc_bdev_error_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_error_inject_error_ctx {
	char * name;
	enum rpc_bdev_error_inject_io_type io_type;
	enum rpc_bdev_error_inject_error_type error_type;
	uint32_t nvme_sct;
	uint32_t nvme_sc;
	uint32_t num;
	uint64_t queue_depth;
	uint64_t corrupt_offset;
	uint8_t corrupt_value;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_error_inject_error(struct rpc_bdev_error_inject_error_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_error_resume_pending_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_error_resume_pending(struct rpc_bdev_error_resume_pending_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_iscsi_set_options_ctx {
	uint64_t timeout_sec;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_iscsi_set_options(struct rpc_bdev_iscsi_set_options_ctx *req)
{
}

struct rpc_bdev_iscsi_create_ctx {
	char * name;
	char * initiator_iqn;
	char * url;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_iscsi_create(struct rpc_bdev_iscsi_create_ctx *req)
{
	free(req->name);
	free(req->initiator_iqn);
	free(req->url);
}

struct rpc_bdev_iscsi_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_iscsi_delete(struct rpc_bdev_iscsi_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ftl_create_ctx {
	char * name;
	char * base_bdev;
	char * cache;
	struct spdk_uuid uuid;
	char * core_mask;
	uint64_t overprovisioning;
	bool fast_shutdown;
	uint64_t l2p_dram_limit;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ftl_create(struct rpc_bdev_ftl_create_ctx *req)
{
	free(req->name);
	free(req->base_bdev);
	free(req->cache);
	free(req->core_mask);
}

struct rpc_bdev_ftl_delete_ctx {
	char * name;
	bool fast_shutdown;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ftl_delete(struct rpc_bdev_ftl_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ftl_unmap_ctx {
	char * name;
	uint64_t lba;
	uint64_t num_blocks;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ftl_unmap(struct rpc_bdev_ftl_unmap_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ftl_get_stats_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ftl_get_stats(struct rpc_bdev_ftl_get_stats_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ftl_get_properties_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ftl_get_properties(struct rpc_bdev_ftl_get_properties_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_ftl_set_property_ctx {
	char * name;
	char * ftl_property;
	char * value;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_ftl_set_property(struct rpc_bdev_ftl_set_property_ctx *req)
{
	free(req->name);
	free(req->ftl_property);
	free(req->value);
}

struct rpc_bdev_passthru_create_ctx {
	char * name;
	char * base_bdev_name;
	struct spdk_uuid uuid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_passthru_create(struct rpc_bdev_passthru_create_ctx *req)
{
	free(req->name);
	free(req->base_bdev_name);
}

struct rpc_bdev_passthru_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_passthru_delete(struct rpc_bdev_passthru_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_xnvme_create_ctx {
	char * name;
	char * filename;
	char * io_mechanism;
	bool conserve_cpu;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_xnvme_create(struct rpc_bdev_xnvme_create_ctx *req)
{
	free(req->name);
	free(req->filename);
	free(req->io_mechanism);
}

struct rpc_bdev_xnvme_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_xnvme_delete(struct rpc_bdev_xnvme_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_virtio_attach_controller_ctx {
	char * name;
	enum rpc_bdev_virtio_trtype trtype;
	char * traddr;
	enum rpc_bdev_virtio_dev_type dev_type;
	uint32_t vq_count;
	uint32_t vq_size;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_virtio_attach_controller(struct rpc_bdev_virtio_attach_controller_ctx *req)
{
	free(req->name);
	free(req->traddr);
}


struct rpc_bdev_virtio_detach_controller_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_virtio_detach_controller(struct rpc_bdev_virtio_detach_controller_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_virtio_blk_set_hotplug_ctx {
	bool enable;
	uint64_t period_us;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_virtio_blk_set_hotplug(struct rpc_bdev_virtio_blk_set_hotplug_ctx *req)
{
}

struct rpc_iscsi_set_options_ctx {
	char * auth_file;
	char * node_base;
	int32_t nop_timeout;
	int32_t nop_in_interval;
	bool disable_chap;
	bool require_chap;
	bool mutual_chap;
	int32_t chap_group;
	uint32_t max_sessions;
	uint32_t max_queue_depth;
	uint32_t max_connections_per_session;
	uint32_t default_time2wait;
	uint32_t default_time2retain;
	uint32_t first_burst_length;
	bool immediate_data;
	uint32_t error_recovery_level;
	bool allow_duplicated_isid;
	uint32_t max_large_datain_per_connection;
	uint32_t max_r2t_per_connection;
	uint32_t pdu_pool_size;
	uint32_t immediate_data_pool_size;
	uint32_t data_out_pool_size;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_set_options(struct rpc_iscsi_set_options_ctx *req)
{
	free(req->auth_file);
	free(req->node_base);
}



struct rpc_iscsi_set_discovery_auth_ctx {
	bool disable_chap;
	bool require_chap;
	bool mutual_chap;
	int32_t chap_group;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_set_discovery_auth(struct rpc_iscsi_set_discovery_auth_ctx *req)
{
}

struct rpc_iscsi_create_auth_group_ctx {
	int32_t tag;
	struct rpc_iscsi_auth_secrets secrets;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_create_auth_group(struct rpc_iscsi_create_auth_group_ctx *req)
{
	free_rpc_iscsi_auth_secrets(&req->secrets);
}

struct rpc_iscsi_delete_auth_group_ctx {
	int32_t tag;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_delete_auth_group(struct rpc_iscsi_delete_auth_group_ctx *req)
{
}


struct rpc_iscsi_auth_group_add_secret_ctx {
	int32_t tag;
	char * user;
	char * secret;
	char * muser;
	char * msecret;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_auth_group_add_secret(struct rpc_iscsi_auth_group_add_secret_ctx *req)
{
	free(req->user);
	free(req->secret);
	free(req->muser);
	free(req->msecret);
}

struct rpc_iscsi_auth_group_remove_secret_ctx {
	int32_t tag;
	char * user;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_auth_group_remove_secret(struct rpc_iscsi_auth_group_remove_secret_ctx *req)
{
	free(req->user);
}


struct rpc_iscsi_create_initiator_group_ctx {
	int32_t tag;
	struct rpc_iscsi_initiators initiators;
	struct rpc_iscsi_netmasks netmasks;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_create_initiator_group(struct rpc_iscsi_create_initiator_group_ctx *req)
{
	free_rpc_iscsi_initiators(&req->initiators);
	free_rpc_iscsi_netmasks(&req->netmasks);
}

struct rpc_iscsi_delete_initiator_group_ctx {
	int32_t tag;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_delete_initiator_group(struct rpc_iscsi_delete_initiator_group_ctx *req)
{
}

struct rpc_iscsi_initiator_group_add_initiators_ctx {
	int32_t tag;
	struct rpc_iscsi_initiators initiators;
	struct rpc_iscsi_netmasks netmasks;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_initiator_group_add_initiators(struct rpc_iscsi_initiator_group_add_initiators_ctx *req)
{
	free_rpc_iscsi_initiators(&req->initiators);
	free_rpc_iscsi_netmasks(&req->netmasks);
}

struct rpc_iscsi_initiator_group_remove_initiators_ctx {
	int32_t tag;
	struct rpc_iscsi_initiators initiators;
	struct rpc_iscsi_netmasks netmasks;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_initiator_group_remove_initiators(struct rpc_iscsi_initiator_group_remove_initiators_ctx *req)
{
	free_rpc_iscsi_initiators(&req->initiators);
	free_rpc_iscsi_netmasks(&req->netmasks);
}


struct rpc_iscsi_create_target_node_ctx {
	char * name;
	char * alias_name;
	struct rpc_iscsi_pg_ig_maps pg_ig_maps;
	struct rpc_scsi_luns luns;
	int32_t queue_depth;
	bool disable_chap;
	bool require_chap;
	bool mutual_chap;
	int32_t chap_group;
	bool header_digest;
	bool data_digest;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_create_target_node(struct rpc_iscsi_create_target_node_ctx *req)
{
	free(req->name);
	free(req->alias_name);
	free_rpc_iscsi_pg_ig_maps(&req->pg_ig_maps);
	free_rpc_scsi_luns(&req->luns);
}

struct rpc_iscsi_target_node_set_auth_ctx {
	char * name;
	bool disable_chap;
	bool require_chap;
	bool mutual_chap;
	int32_t chap_group;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_target_node_set_auth(struct rpc_iscsi_target_node_set_auth_ctx *req)
{
	free(req->name);
}

struct rpc_iscsi_target_node_add_pg_ig_maps_ctx {
	char * name;
	struct rpc_iscsi_pg_ig_maps pg_ig_maps;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_target_node_add_pg_ig_maps(struct rpc_iscsi_target_node_add_pg_ig_maps_ctx *req)
{
	free(req->name);
	free_rpc_iscsi_pg_ig_maps(&req->pg_ig_maps);
}

struct rpc_iscsi_target_node_remove_pg_ig_maps_ctx {
	char * name;
	struct rpc_iscsi_pg_ig_maps pg_ig_maps;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_target_node_remove_pg_ig_maps(struct rpc_iscsi_target_node_remove_pg_ig_maps_ctx *req)
{
	free(req->name);
	free_rpc_iscsi_pg_ig_maps(&req->pg_ig_maps);
}

struct rpc_iscsi_delete_target_node_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_delete_target_node(struct rpc_iscsi_delete_target_node_ctx *req)
{
	free(req->name);
}


struct rpc_iscsi_create_portal_group_ctx {
	int32_t tag;
	struct rpc_iscsi_portals portals;
	bool is_private;
	bool wait;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_create_portal_group(struct rpc_iscsi_create_portal_group_ctx *req)
{
	free_rpc_iscsi_portals(&req->portals);
}

struct rpc_iscsi_start_portal_group_ctx {
	int32_t tag;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_start_portal_group(struct rpc_iscsi_start_portal_group_ctx *req)
{
}

struct rpc_iscsi_delete_portal_group_ctx {
	int32_t tag;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_delete_portal_group(struct rpc_iscsi_delete_portal_group_ctx *req)
{
}

struct rpc_iscsi_portal_group_set_auth_ctx {
	int32_t tag;
	bool disable_chap;
	bool require_chap;
	bool mutual_chap;
	int32_t chap_group;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_portal_group_set_auth(struct rpc_iscsi_portal_group_set_auth_ctx *req)
{
}



struct rpc_iscsi_target_node_add_lun_ctx {
	char * name;
	char * bdev_name;
	int32_t lun_id;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_target_node_add_lun(struct rpc_iscsi_target_node_add_lun_ctx *req)
{
	free(req->name);
	free(req->bdev_name);
}

struct rpc_iscsi_target_node_set_redirect_ctx {
	char * name;
	int32_t pg_tag;
	char * redirect_host;
	char * redirect_port;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_target_node_set_redirect(struct rpc_iscsi_target_node_set_redirect_ctx *req)
{
	free(req->name);
	free(req->redirect_host);
	free(req->redirect_port);
}

struct rpc_iscsi_target_node_request_logout_ctx {
	char * name;
	int32_t pg_tag;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_target_node_request_logout(struct rpc_iscsi_target_node_request_logout_ctx *req)
{
	free(req->name);
}

struct rpc_iscsi_enable_histogram_ctx {
	char * name;
	bool enable;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_enable_histogram(struct rpc_iscsi_enable_histogram_ctx *req)
{
	free(req->name);
}

struct rpc_iscsi_get_histogram_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iscsi_get_histogram(struct rpc_iscsi_get_histogram_ctx *req)
{
	free(req->name);
}

struct rpc_nvmf_create_target_ctx {
	char * name;
	uint32_t max_subsystems;
	uint32_t discovery_filter;
	enum rpc_nvmf_dup_host_policy dup_host_policy;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_create_target(struct rpc_nvmf_create_target_ctx *req)
{
	free(req->name);
}

struct rpc_nvmf_delete_target_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_delete_target(struct rpc_nvmf_delete_target_ctx *req)
{
	free(req->name);
}


struct rpc_nvmf_create_transport_ctx {
	char * trtype;
	char * tgt_name;
	uint16_t max_queue_depth;
	uint16_t max_io_qpairs_per_ctrlr;
	uint32_t in_capsule_data_size;
	uint32_t max_io_size;
	uint32_t io_unit_size;
	uint32_t max_aq_depth;
	uint32_t num_shared_buffers;
	uint32_t buf_cache_size;
	uint32_t iobuf_small_cache_size;
	uint32_t iobuf_large_cache_size;
	bool zcopy;
	int32_t num_cqe;
	uint32_t max_srq_depth;
	bool no_srq;
	bool c2h_success;
	bool dif_insert_or_strip;
	uint32_t sock_priority;
	int32_t acceptor_backlog;
	uint32_t abort_timeout_sec;
	bool no_wr_batching;
	uint16_t control_msg_num;
	bool disable_mappable_bar0;
	bool disable_adaptive_irq;
	bool disable_shadow_doorbells;
	uint32_t acceptor_poll_rate;
	uint32_t ack_timeout;
	uint32_t data_wr_pool_size;
	bool disable_command_passthru;
	uint16_t kas;
	uint32_t min_kato;
	struct rpc_oncs_features masked_oncs;
	struct rpc_fuses_features masked_fuses;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_create_transport(struct rpc_nvmf_create_transport_ctx *req)
{
	free(req->trtype);
	free(req->tgt_name);
	free_rpc_oncs_features(&req->masked_oncs);
	free_rpc_fuses_features(&req->masked_fuses);
}

struct rpc_nvmf_get_subsystems_ctx {
	char * nqn;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_get_subsystems(struct rpc_nvmf_get_subsystems_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
}

struct rpc_nvmf_create_subsystem_ctx {
	char * nqn;
	char * tgt_name;
	char * serial_number;
	char * model_number;
	uint32_t max_namespaces;
	bool allow_any_host;
	bool ana_reporting;
	uint16_t min_cntlid;
	uint16_t max_cntlid;
	uint64_t max_discard_size_kib;
	uint64_t max_write_zeroes_size_kib;
	uint32_t dmrsl;
	uint8_t wzsl;
	bool passthrough;
	bool enable_nssr;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_create_subsystem(struct rpc_nvmf_create_subsystem_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
	free(req->serial_number);
	free(req->model_number);
}

struct rpc_nvmf_delete_subsystem_ctx {
	char * nqn;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_delete_subsystem(struct rpc_nvmf_delete_subsystem_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
}

struct rpc_nvmf_subsystem_add_listener_ctx {
	char * nqn;
	char * tgt_name;
	struct rpc_nvmf_listen_address listen_address;
	bool secure_channel;
	enum rpc_nvme_ana_state ana_state;
	char * sock_impl;
	int32_t numa_id;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_add_listener(struct rpc_nvmf_subsystem_add_listener_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
	free(req->sock_impl);
	free_rpc_nvmf_listen_address(&req->listen_address);
}

struct rpc_nvmf_subsystem_remove_listener_ctx {
	char * nqn;
	char * tgt_name;
	struct rpc_nvmf_listen_address listen_address;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_remove_listener(struct rpc_nvmf_subsystem_remove_listener_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
	free_rpc_nvmf_listen_address(&req->listen_address);
}

struct rpc_nvmf_subsystem_listener_set_ana_state_ctx {
	char * nqn;
	char * tgt_name;
	struct rpc_nvmf_listen_address listen_address;
	enum rpc_nvme_ana_state ana_state;
	uint32_t anagrpid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_listener_set_ana_state(struct rpc_nvmf_subsystem_listener_set_ana_state_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
	free_rpc_nvmf_listen_address(&req->listen_address);
}

struct rpc_nvmf_subsystem_add_ns_ctx {
	char * nqn;
	struct rpc_nvmf_namespace namespace;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_add_ns(struct rpc_nvmf_subsystem_add_ns_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
	free_rpc_nvmf_namespace(&req->namespace);
}

struct rpc_nvmf_subsystem_remove_ns_ctx {
	char * nqn;
	uint32_t nsid;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_remove_ns(struct rpc_nvmf_subsystem_remove_ns_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
}

struct rpc_nvmf_subsystem_set_ns_ana_group_ctx {
	char * nqn;
	uint32_t nsid;
	uint32_t anagrpid;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_set_ns_ana_group(struct rpc_nvmf_subsystem_set_ns_ana_group_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
}

struct rpc_nvmf_subsystem_add_host_ctx {
	char * nqn;
	char * host;
	char * tgt_name;
	char * psk;
	char * dhchap_key;
	char * dhchap_ctrlr_key;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_add_host(struct rpc_nvmf_subsystem_add_host_ctx *req)
{
	free(req->nqn);
	free(req->host);
	free(req->tgt_name);
	free(req->psk);
	free(req->dhchap_key);
	free(req->dhchap_ctrlr_key);
}

struct rpc_nvmf_subsystem_remove_host_ctx {
	char * nqn;
	char * host;
	char * tgt_name;
	uint64_t timeout_ms;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_remove_host(struct rpc_nvmf_subsystem_remove_host_ctx *req)
{
	free(req->nqn);
	free(req->host);
	free(req->tgt_name);
}

struct rpc_nvmf_subsystem_allow_any_host_ctx {
	char * nqn;
	bool allow_any_host;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_allow_any_host(struct rpc_nvmf_subsystem_allow_any_host_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
}

struct rpc_nvmf_subsystem_set_keys_ctx {
	char * nqn;
	char * host;
	char * tgt_name;
	char * dhchap_key;
	char * dhchap_ctrlr_key;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_set_keys(struct rpc_nvmf_subsystem_set_keys_ctx *req)
{
	free(req->nqn);
	free(req->host);
	free(req->tgt_name);
	free(req->dhchap_key);
	free(req->dhchap_ctrlr_key);
}

struct rpc_nvmf_subsystem_get_controllers_ctx {
	char * nqn;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_get_controllers(struct rpc_nvmf_subsystem_get_controllers_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
}

struct rpc_nvmf_subsystem_get_qpairs_ctx {
	char * nqn;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_get_qpairs(struct rpc_nvmf_subsystem_get_qpairs_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
}

struct rpc_nvmf_subsystem_get_listeners_ctx {
	char * nqn;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_subsystem_get_listeners(struct rpc_nvmf_subsystem_get_listeners_ctx *req)
{
	free(req->nqn);
	free(req->tgt_name);
}

struct rpc_nvmf_ns_add_host_ctx {
	char * nqn;
	uint32_t nsid;
	char * host;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_ns_add_host(struct rpc_nvmf_ns_add_host_ctx *req)
{
	free(req->nqn);
	free(req->host);
	free(req->tgt_name);
}

struct rpc_nvmf_ns_remove_host_ctx {
	char * nqn;
	uint32_t nsid;
	char * host;
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_ns_remove_host(struct rpc_nvmf_ns_remove_host_ctx *req)
{
	free(req->nqn);
	free(req->host);
	free(req->tgt_name);
}

struct rpc_nvmf_set_max_subsystems_ctx {
	uint32_t max_subsystems;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_set_max_subsystems(struct rpc_nvmf_set_max_subsystems_ctx *req)
{
}

struct rpc_nvmf_discovery_add_referral_ctx {
	char * tgt_name;
	struct rpc_nvmf_listen_address address;
	bool secure_channel;
	char * subnqn;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_discovery_add_referral(struct rpc_nvmf_discovery_add_referral_ctx *req)
{
	free(req->tgt_name);
	free(req->subnqn);
	free_rpc_nvmf_listen_address(&req->address);
}

struct rpc_nvmf_discovery_remove_referral_ctx {
	char * tgt_name;
	struct rpc_nvmf_listen_address address;
	char * subnqn;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_discovery_remove_referral(struct rpc_nvmf_discovery_remove_referral_ctx *req)
{
	free(req->tgt_name);
	free(req->subnqn);
	free_rpc_nvmf_listen_address(&req->address);
}

struct rpc_nvmf_discovery_get_referrals_ctx {
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_discovery_get_referrals(struct rpc_nvmf_discovery_get_referrals_ctx *req)
{
	free(req->tgt_name);
}

struct rpc_nvmf_set_config_ctx {
	struct rpc_nvmf_admin_cmd_passthru admin_cmd_passthru;
	char * poll_groups_mask;
	char * discovery_filter;
	struct rpc_dhchap_digests dhchap_digests;
	struct rpc_dhchap_dhgroups dhchap_dhgroups;
	enum rpc_nvmf_dup_host_policy dup_host_policy;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_set_config(struct rpc_nvmf_set_config_ctx *req)
{
	free(req->poll_groups_mask);
	free(req->discovery_filter);
	free_rpc_nvmf_admin_cmd_passthru(&req->admin_cmd_passthru);
	free_rpc_dhchap_digests(&req->dhchap_digests);
	free_rpc_dhchap_dhgroups(&req->dhchap_dhgroups);
}

struct rpc_nvmf_get_transports_ctx {
	char * tgt_name;
	char * trtype;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_get_transports(struct rpc_nvmf_get_transports_ctx *req)
{
	free(req->tgt_name);
	free(req->trtype);
}

struct rpc_nvmf_get_stats_ctx {
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_get_stats(struct rpc_nvmf_get_stats_ctx *req)
{
	free(req->tgt_name);
}

struct rpc_nvmf_set_crdt_ctx {
	uint16_t crdt1;
	uint16_t crdt2;
	uint16_t crdt3;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_set_crdt(struct rpc_nvmf_set_crdt_ctx *req)
{
}

struct rpc_vfu_tgt_set_base_path_ctx {
	char * path;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vfu_tgt_set_base_path(struct rpc_vfu_tgt_set_base_path_ctx *req)
{
	free(req->path);
}

struct rpc_vfu_virtio_delete_endpoint_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vfu_virtio_delete_endpoint(struct rpc_vfu_virtio_delete_endpoint_ctx *req)
{
	free(req->name);
}

struct rpc_vfu_virtio_create_blk_endpoint_ctx {
	char * name;
	char * bdev_name;
	char * cpumask;
	uint16_t num_queues;
	uint16_t qsize;
	bool packed_ring;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vfu_virtio_create_blk_endpoint(struct rpc_vfu_virtio_create_blk_endpoint_ctx *req)
{
	free(req->name);
	free(req->bdev_name);
	free(req->cpumask);
}

struct rpc_vfu_virtio_scsi_add_target_ctx {
	char * name;
	uint8_t scsi_target_num;
	char * bdev_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vfu_virtio_scsi_add_target(struct rpc_vfu_virtio_scsi_add_target_ctx *req)
{
	free(req->name);
	free(req->bdev_name);
}

struct rpc_vfu_virtio_scsi_remove_target_ctx {
	char * name;
	uint8_t scsi_target_num;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vfu_virtio_scsi_remove_target(struct rpc_vfu_virtio_scsi_remove_target_ctx *req)
{
	free(req->name);
}

struct rpc_vfu_virtio_create_scsi_endpoint_ctx {
	char * name;
	char * cpumask;
	uint16_t num_io_queues;
	uint16_t qsize;
	bool packed_ring;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vfu_virtio_create_scsi_endpoint(struct rpc_vfu_virtio_create_scsi_endpoint_ctx *req)
{
	free(req->name);
	free(req->cpumask);
}

struct rpc_vfu_virtio_create_fs_endpoint_ctx {
	char * name;
	char * fsdev_name;
	char * tag;
	char * cpumask;
	uint16_t num_queues;
	uint16_t qsize;
	bool packed_ring;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vfu_virtio_create_fs_endpoint(struct rpc_vfu_virtio_create_fs_endpoint_ctx *req)
{
	free(req->name);
	free(req->fsdev_name);
	free(req->tag);
	free(req->cpumask);
}

struct rpc_vhost_controller_set_coalescing_ctx {
	char * ctrlr;
	uint32_t delay_base_us;
	uint32_t iops_threshold;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vhost_controller_set_coalescing(struct rpc_vhost_controller_set_coalescing_ctx *req)
{
	free(req->ctrlr);
}

struct rpc_vhost_create_scsi_controller_ctx {
	char * ctrlr;
	char * cpumask;
	bool delay;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vhost_create_scsi_controller(struct rpc_vhost_create_scsi_controller_ctx *req)
{
	free(req->ctrlr);
	free(req->cpumask);
}

struct rpc_vhost_start_scsi_controller_ctx {
	char * ctrlr;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vhost_start_scsi_controller(struct rpc_vhost_start_scsi_controller_ctx *req)
{
	free(req->ctrlr);
}

struct rpc_vhost_scsi_controller_add_target_ctx {
	char * ctrlr;
	int32_t scsi_target_num;
	char * bdev_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vhost_scsi_controller_add_target(struct rpc_vhost_scsi_controller_add_target_ctx *req)
{
	free(req->ctrlr);
	free(req->bdev_name);
}

struct rpc_vhost_scsi_controller_remove_target_ctx {
	char * ctrlr;
	uint32_t scsi_target_num;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vhost_scsi_controller_remove_target(struct rpc_vhost_scsi_controller_remove_target_ctx *req)
{
	free(req->ctrlr);
}

struct rpc_virtio_blk_create_transport_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_virtio_blk_create_transport(struct rpc_virtio_blk_create_transport_ctx *req)
{
	free(req->name);
}

struct rpc_virtio_blk_get_transports_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_virtio_blk_get_transports(struct rpc_virtio_blk_get_transports_ctx *req)
{
	free(req->name);
}

struct rpc_vhost_create_blk_controller_ctx {
	char * ctrlr;
	char * dev_name;
	bool readonly;
	bool packed_ring;
	char * cpumask;
	char * transport;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vhost_create_blk_controller(struct rpc_vhost_create_blk_controller_ctx *req)
{
	free(req->ctrlr);
	free(req->dev_name);
	free(req->cpumask);
	free(req->transport);
}

struct rpc_vhost_get_controllers_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vhost_get_controllers(struct rpc_vhost_get_controllers_ctx *req)
{
	free(req->name);
}

struct rpc_vhost_delete_controller_ctx {
	char * ctrlr;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vhost_delete_controller(struct rpc_vhost_delete_controller_ctx *req)
{
	free(req->ctrlr);
}

struct rpc_bdev_lvol_create_lvstore_ctx {
	char * bdev_name;
	char * lvs_name;
	uint32_t cluster_sz;
	char * clear_method;
	uint32_t num_md_pages_per_cluster_ratio;
	uint32_t md_page_size;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_create_lvstore(struct rpc_bdev_lvol_create_lvstore_ctx *req)
{
	free(req->bdev_name);
	free(req->lvs_name);
	free(req->clear_method);
}

struct rpc_bdev_lvol_delete_lvstore_ctx {
	char * uuid;
	char * lvs_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_delete_lvstore(struct rpc_bdev_lvol_delete_lvstore_ctx *req)
{
	free(req->uuid);
	free(req->lvs_name);
}

struct rpc_bdev_lvol_get_lvstores_ctx {
	char * uuid;
	char * lvs_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_get_lvstores(struct rpc_bdev_lvol_get_lvstores_ctx *req)
{
	free(req->uuid);
	free(req->lvs_name);
}

struct rpc_bdev_lvol_rename_lvstore_ctx {
	char * old_name;
	char * new_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_rename_lvstore(struct rpc_bdev_lvol_rename_lvstore_ctx *req)
{
	free(req->old_name);
	free(req->new_name);
}

struct rpc_bdev_lvol_grow_lvstore_ctx {
	char * uuid;
	char * lvs_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_grow_lvstore(struct rpc_bdev_lvol_grow_lvstore_ctx *req)
{
	free(req->uuid);
	free(req->lvs_name);
}

struct rpc_bdev_lvol_create_ctx {
	char * lvol_name;
	uint64_t size_in_mib;
	bool thin_provision;
	char * uuid;
	char * lvs_name;
	char * clear_method;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_create(struct rpc_bdev_lvol_create_ctx *req)
{
	free(req->lvol_name);
	free(req->uuid);
	free(req->lvs_name);
	free(req->clear_method);
}

struct rpc_bdev_lvol_snapshot_ctx {
	char * lvol_name;
	char * snapshot_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_snapshot(struct rpc_bdev_lvol_snapshot_ctx *req)
{
	free(req->lvol_name);
	free(req->snapshot_name);
}

struct rpc_bdev_lvol_clone_ctx {
	char * snapshot_name;
	char * clone_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_clone(struct rpc_bdev_lvol_clone_ctx *req)
{
	free(req->snapshot_name);
	free(req->clone_name);
}

struct rpc_bdev_lvol_clone_bdev_ctx {
	char * bdev;
	char * lvs_name;
	char * clone_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_clone_bdev(struct rpc_bdev_lvol_clone_bdev_ctx *req)
{
	free(req->bdev);
	free(req->lvs_name);
	free(req->clone_name);
}

struct rpc_bdev_lvol_rename_ctx {
	char * old_name;
	char * new_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_rename(struct rpc_bdev_lvol_rename_ctx *req)
{
	free(req->old_name);
	free(req->new_name);
}

struct rpc_bdev_lvol_resize_ctx {
	char * name;
	uint64_t size_in_mib;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_resize(struct rpc_bdev_lvol_resize_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_lvol_set_read_only_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_set_read_only(struct rpc_bdev_lvol_set_read_only_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_lvol_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_delete(struct rpc_bdev_lvol_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_lvol_inflate_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_inflate(struct rpc_bdev_lvol_inflate_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_lvol_decouple_parent_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_decouple_parent(struct rpc_bdev_lvol_decouple_parent_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_lvol_get_lvols_ctx {
	char * lvs_uuid;
	char * lvs_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_get_lvols(struct rpc_bdev_lvol_get_lvols_ctx *req)
{
	free(req->lvs_uuid);
	free(req->lvs_name);
}

struct rpc_bdev_lvol_set_parent_ctx {
	char * lvol_name;
	char * parent_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_set_parent(struct rpc_bdev_lvol_set_parent_ctx *req)
{
	free(req->lvol_name);
	free(req->parent_name);
}

struct rpc_bdev_lvol_set_parent_bdev_ctx {
	char * lvol_name;
	char * parent_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_set_parent_bdev(struct rpc_bdev_lvol_set_parent_bdev_ctx *req)
{
	free(req->lvol_name);
	free(req->parent_name);
}

struct rpc_bdev_lvol_start_shallow_copy_ctx {
	char * src_lvol_name;
	char * dst_bdev_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_start_shallow_copy(struct rpc_bdev_lvol_start_shallow_copy_ctx *req)
{
	free(req->src_lvol_name);
	free(req->dst_bdev_name);
}

struct rpc_bdev_lvol_check_shallow_copy_ctx {
	uint32_t operation_id;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_lvol_check_shallow_copy(struct rpc_bdev_lvol_check_shallow_copy_ctx *req)
{
}

struct rpc_bdev_raid_set_options_ctx {
	uint32_t process_window_size_kb;
	uint32_t process_max_bandwidth_mb_sec;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_raid_set_options(struct rpc_bdev_raid_set_options_ctx *req)
{
}

struct rpc_bdev_raid_get_bdevs_ctx {
	enum rpc_bdev_raid_state category;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_raid_get_bdevs(struct rpc_bdev_raid_get_bdevs_ctx *req)
{
}

struct rpc_bdev_raid_create_ctx {
	char * name;
	uint32_t strip_size_kb;
	enum rpc_bdev_raid_level raid_level;
	struct rpc_raid_base_bdevs base_bdevs;
	struct spdk_uuid uuid;
	bool superblock;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_raid_create(struct rpc_bdev_raid_create_ctx *req)
{
	free(req->name);
	free_rpc_raid_base_bdevs(&req->base_bdevs);
}

struct rpc_bdev_raid_delete_ctx {
	char * name;
	bool clear_sb;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_raid_delete(struct rpc_bdev_raid_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_raid_add_base_bdev_ctx {
	char * raid_bdev;
	char * base_bdev;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_raid_add_base_bdev(struct rpc_bdev_raid_add_base_bdev_ctx *req)
{
	free(req->raid_bdev);
	free(req->base_bdev);
}

struct rpc_bdev_raid_remove_base_bdev_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_raid_remove_base_bdev(struct rpc_bdev_raid_remove_base_bdev_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_split_create_ctx {
	char * base_bdev;
	uint32_t split_count;
	uint64_t split_size_mb;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_split_create(struct rpc_bdev_split_create_ctx *req)
{
	free(req->base_bdev);
}

struct rpc_bdev_split_delete_ctx {
	char * base_bdev;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_split_delete(struct rpc_bdev_split_delete_ctx *req)
{
	free(req->base_bdev);
}

struct rpc_bdev_uring_create_ctx {
	char * filename;
	char * name;
	uint32_t block_size;
	struct spdk_uuid uuid;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_uring_create(struct rpc_bdev_uring_create_ctx *req)
{
	free(req->filename);
	free(req->name);
}

struct rpc_bdev_uring_rescan_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_uring_rescan(struct rpc_bdev_uring_rescan_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_uring_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_uring_delete(struct rpc_bdev_uring_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_opal_init_ctx {
	char * nvme_ctrlr_name;
	char * password;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_opal_init(struct rpc_bdev_nvme_opal_init_ctx *req)
{
	free(req->nvme_ctrlr_name);
	free(req->password);
}

struct rpc_bdev_nvme_opal_revert_ctx {
	char * nvme_ctrlr_name;
	char * password;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_opal_revert(struct rpc_bdev_nvme_opal_revert_ctx *req)
{
	free(req->nvme_ctrlr_name);
	free(req->password);
}

struct rpc_bdev_opal_create_ctx {
	char * nvme_ctrlr_name;
	uint32_t nsid;
	uint16_t locking_range_id;
	uint64_t range_start;
	uint64_t range_length;
	char * password;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_opal_create(struct rpc_bdev_opal_create_ctx *req)
{
	free(req->nvme_ctrlr_name);
	free(req->password);
}

struct rpc_bdev_opal_get_info_ctx {
	char * bdev_name;
	char * password;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_opal_get_info(struct rpc_bdev_opal_get_info_ctx *req)
{
	free(req->bdev_name);
	free(req->password);
}

struct rpc_bdev_opal_delete_ctx {
	char * bdev_name;
	char * password;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_opal_delete(struct rpc_bdev_opal_delete_ctx *req)
{
	free(req->bdev_name);
	free(req->password);
}

struct rpc_bdev_opal_new_user_ctx {
	char * bdev_name;
	char * admin_password;
	uint16_t user_id;
	char * user_password;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_opal_new_user(struct rpc_bdev_opal_new_user_ctx *req)
{
	free(req->bdev_name);
	free(req->admin_password);
	free(req->user_password);
}

struct rpc_bdev_opal_set_lock_state_ctx {
	char * bdev_name;
	uint16_t user_id;
	char * password;
	char * lock_state;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_opal_set_lock_state(struct rpc_bdev_opal_set_lock_state_ctx *req)
{
	free(req->bdev_name);
	free(req->password);
	free(req->lock_state);
}


struct rpc_notify_get_notifications_ctx {
	uint64_t id;
	uint64_t max;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_notify_get_notifications(struct rpc_notify_get_notifications_ctx *req)
{
}

struct rpc_keyring_file_add_key_ctx {
	char * name;
	char * path;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_keyring_file_add_key(struct rpc_keyring_file_add_key_ctx *req)
{
	free(req->name);
	free(req->path);
}

struct rpc_keyring_file_remove_key_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_keyring_file_remove_key(struct rpc_keyring_file_remove_key_ctx *req)
{
	free(req->name);
}


struct rpc_keyring_linux_set_options_ctx {
	bool enable;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_keyring_linux_set_options(struct rpc_keyring_linux_set_options_ctx *req)
{
}

struct rpc_ublk_create_target_ctx {
	char * cpumask;
	bool disable_user_copy;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_ublk_create_target(struct rpc_ublk_create_target_ctx *req)
{
	free(req->cpumask);
}


struct rpc_ublk_start_disk_ctx {
	char * bdev_name;
	uint32_t ublk_id;
	uint32_t queue_depth;
	uint32_t num_queues;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_ublk_start_disk(struct rpc_ublk_start_disk_ctx *req)
{
	free(req->bdev_name);
}

struct rpc_ublk_recover_disk_ctx {
	char * bdev_name;
	uint32_t ublk_id;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_ublk_recover_disk(struct rpc_ublk_recover_disk_ctx *req)
{
	free(req->bdev_name);
}

struct rpc_ublk_stop_disk_ctx {
	uint32_t ublk_id;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_ublk_stop_disk(struct rpc_ublk_stop_disk_ctx *req)
{
}

struct rpc_ublk_get_disks_ctx {
	uint32_t ublk_id;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_ublk_get_disks(struct rpc_ublk_get_disks_ctx *req)
{
}

struct rpc_nbd_start_disk_ctx {
	char * bdev_name;
	char * nbd_device;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nbd_start_disk(struct rpc_nbd_start_disk_ctx *req)
{
	free(req->bdev_name);
	free(req->nbd_device);
}

struct rpc_nbd_stop_disk_ctx {
	char * nbd_device;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nbd_stop_disk(struct rpc_nbd_stop_disk_ctx *req)
{
	free(req->nbd_device);
}

struct rpc_nbd_get_disks_ctx {
	char * nbd_device;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nbd_get_disks(struct rpc_nbd_get_disks_ctx *req)
{
	free(req->nbd_device);
}

struct rpc_sock_impl_get_options_ctx {
	char * impl_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_sock_impl_get_options(struct rpc_sock_impl_get_options_ctx *req)
{
	free(req->impl_name);
}

struct rpc_sock_impl_set_options_ctx {
	char * impl_name;
	uint32_t recv_buf_size;
	uint32_t send_buf_size;
	bool enable_recv_pipe;
	bool enable_quickack;
	uint32_t enable_placement_id;
	bool enable_zerocopy_send_server;
	bool enable_zerocopy_send_client;
	uint32_t zerocopy_threshold;
	uint32_t tls_version;
	bool enable_ktls;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_sock_impl_set_options(struct rpc_sock_impl_set_options_ctx *req)
{
	free(req->impl_name);
}

struct rpc_sock_set_default_impl_ctx {
	char * impl_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_sock_set_default_impl(struct rpc_sock_set_default_impl_ctx *req)
{
	free(req->impl_name);
}


struct rpc_bdev_nvme_send_cmd_ctx {
	char * name;
	enum rpc_bdev_nvme_cmd_type cmd_type;
	enum rpc_bdev_nvme_data_direction data_direction;
	char * cmdbuf;
	char * data;
	char * metadata;
	uint32_t data_len;
	uint32_t metadata_len;
	uint32_t timeout_ms;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_send_cmd(struct rpc_bdev_nvme_send_cmd_ctx *req)
{
	free(req->name);
	free(req->cmdbuf);
	free(req->data);
	free(req->metadata);
}


struct rpc_vmd_remove_device_ctx {
	char * addr;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_vmd_remove_device(struct rpc_vmd_remove_device_ctx *req)
{
	free(req->addr);
}




struct rpc_bdev_nvme_add_error_injection_ctx {
	char * name;
	enum rpc_bdev_nvme_cmd_type cmd_type;
	uint8_t opc;
	bool do_not_submit;
	uint64_t timeout_in_us;
	uint32_t err_count;
	uint8_t sct;
	uint8_t sc;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_add_error_injection(struct rpc_bdev_nvme_add_error_injection_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_nvme_remove_error_injection_ctx {
	char * name;
	enum rpc_bdev_nvme_cmd_type cmd_type;
	uint8_t opc;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_remove_error_injection(struct rpc_bdev_nvme_remove_error_injection_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_daos_create_ctx {
	char * name;
	char * pool;
	char * cont;
	uint32_t block_size;
	uint64_t num_blocks;
	struct spdk_uuid uuid;
	char * oclass;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_daos_create(struct rpc_bdev_daos_create_ctx *req)
{
	free(req->name);
	free(req->pool);
	free(req->cont);
	free(req->oclass);
}

struct rpc_bdev_daos_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_daos_delete(struct rpc_bdev_daos_delete_ctx *req)
{
	free(req->name);
}

struct rpc_bdev_daos_resize_ctx {
	char * name;
	uint64_t new_size;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_daos_resize(struct rpc_bdev_daos_resize_ctx *req)
{
	free(req->name);
}

struct rpc_iobuf_set_options_ctx {
	uint64_t small_pool_count;
	uint64_t large_pool_count;
	uint32_t small_bufsize;
	uint32_t large_bufsize;
	bool enable_numa;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_iobuf_set_options(struct rpc_iobuf_set_options_ctx *req)
{
}


struct rpc_bdev_nvme_start_mdns_discovery_ctx {
	char * name;
	char * svcname;
	char * hostnqn;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_start_mdns_discovery(struct rpc_bdev_nvme_start_mdns_discovery_ctx *req)
{
	free(req->name);
	free(req->svcname);
	free(req->hostnqn);
}

struct rpc_bdev_nvme_stop_mdns_discovery_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_bdev_nvme_stop_mdns_discovery(struct rpc_bdev_nvme_stop_mdns_discovery_ctx *req)
{
	free(req->name);
}


struct rpc_nvmf_publish_mdns_prr_ctx {
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_publish_mdns_prr(struct rpc_nvmf_publish_mdns_prr_ctx *req)
{
	free(req->tgt_name);
}

struct rpc_nvmf_stop_mdns_prr_ctx {
	char * tgt_name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_nvmf_stop_mdns_prr(struct rpc_nvmf_stop_mdns_prr_ctx *req)
{
	free(req->tgt_name);
}


struct rpc_fsdev_set_opts_ctx {
	uint32_t fsdev_io_pool_size;
	uint32_t fsdev_io_cache_size;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_fsdev_set_opts(struct rpc_fsdev_set_opts_ctx *req)
{
}

struct rpc_fsdev_aio_create_ctx {
	char * name;
	char * root_path;
	bool enable_xattr;
	bool enable_writeback_cache;
	uint32_t max_write;
	bool skip_rw;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_fsdev_aio_create(struct rpc_fsdev_aio_create_ctx *req)
{
	free(req->name);
	free(req->root_path);
}

struct rpc_fsdev_aio_delete_ctx {
	char * name;
	struct spdk_jsonrpc_request *request;
};

static inline void
free_rpc_fsdev_aio_delete(struct rpc_fsdev_aio_delete_ctx *req)
{
	free(req->name);
}


#ifdef __cplusplus
}
#endif

#endif /* SPDK_RPC_AUTOGEN_H_ */
